# Kerf

浏览器视频剪辑器，**导出在客户端完成**。服务端导出不在范围内，不要提议。

完整方案：[docs/PLAN.md](docs/PLAN.md)（选型理由、架构、兼容矩阵、决策记录、里程碑）
界面设计稿：[design/kerf-editor-mockup.html](design/kerf-editor-mockup.html)（已定稿，四种状态）

当前阶段：**线上部署已换成真编辑器**（2026-07-30，决策 **D51**）：`pages.yml` 从"把设计稿复制成 index.html"换成 `pnpm gate` + `pnpm build --base=/kerf/`，产物就是站点根；两份设计稿继续跟着发，**`/kerf/kerf-editor-mockup.html` 这个已经分享过的旧链接原样保留**。三条纪律：**`--base` 必须给而且只给构建**（项目页在 `/kerf/` 下，Vite 默认 base 是 `/`，产物里的 `/assets/*.js` 会解析到域名根上——页面出得来、脚本全 404、白屏；写进 `vite.config.ts` 则会连开发地址一起搬走，而 CLAUDE.md 里所有 `?autorun=` 和手验 URL 都是按 `localhost:5173/` 写的），**组装步骤要点名验文件在不在**（`cp` 少一个源文件会让那行失败，但产物少一个 chunk 不会；尤其 `grep -q 'src="/kerf/assets/'`——**base 给错时构建照样成功**，那是部署侧唯一挡得住它的地方），**触发条件用 `paths-ignore` 不用白名单**（白名单漏一条路径的表现是"改了代码但线上没变"，没有任何提示）。验的是**那份真产物**：本地按子路径起静态服务器，在里面跑完整 M0 自检——**83 项断言全部成立 · 14.3 秒**（生产构建里 `__kerfStore` 和 `?autorun=` 都是 dev 限定的，全靠真实点击）。

在此之前：**入点 / 出点标记已落地**（2026-07-30，决策 **D50**）：`Timeline.markIn?` / `markOut?`（左闭右开、时间轴帧），I / O 在播放头打、⌥I / ⌥O 清，导出面板的「范围」从布尔换成三选一且**有标记时默认选标记那一档**。价值同 D43 / D47——**让已经写好的路径可达**：`RenderRange` 从 M0 起就接任意帧区间，而界面上原来只有"整条"和"只导选中片段"（后者**只落在片段边界上**，想导 0:30–1:00 得凑一组刚好跨那段的片段）。三条纪律：**字段在 EDL 里所以必须进撤销栈**（D43 那条的第二次适用，而导出范围本来就会改变成片；放 store 的话刷新就没了，而"圈好一段明天接着导"是主要用法）、**"标记指哪一段"只有 `markedRange()` 一个答案**（只设一端的补全规则散写三处必然会漂，漂了就是"面板说 6 秒、读数说 8 秒"）、**合并键按端给**（连打合成一条撤销，但两端不共用一个键——共用会让 ⌘Z 一下把两端一起撤掉）。越过另一端**拒绝而不夹紧、不顺手清对面**（同 D46），时间轴变短时标记跟着夹、**夹完站不住就整个清掉**（留着就是一个选了必报错的导出选项）。`SNAPSHOT_VERSION` 不动，实测快照里带着标记、version 仍是 5。单测 1047 → **1074**。9 轮注入全红；**浏览器手验当场抓到一个真缺陷**：第一版把播放头从 React 闭包传进 store，`setPlayhead(60)` 之后立刻按 I 打出的是第 0 帧（闭包慢一拍）——D48 的 `freezeAtPlayhead` 早就是"在 store 里读播放头"的形状，没照抄的代价。

在此之前：**J/K/L 变速播放已落地**（2026-07-30，决策 **D49**）：预览的播放状态从 `playing: boolean` 换成 `rate: number`（0 暂停 / 1 常速正放 / 负数倒放），判据全在纯函数 `preview/shuttle.ts` 里。核心决定有三个。**一、一个有符号数而不是"布尔 + 数字"**：后者能构造出"playing 为真而倍率是 0"和"倍率是 4 而 playing 为假"两种自相矛盾的组合，表现分别是"播放中画面纹丝不动"和"暂停了播放头还在跑"；`playing = rate !== 0` 把这一类整个消掉，而它**不进 store、不进撤销栈**（比播放头和选中更彻底——它连项目状态都不是）。**二、变速时不出声，而且这不是"以后再补"**：预览播的是导出会写进成片的**同一份 PCM**（D26），它与墙上时间 1:1，要在 2× 上出声就得让预览混一份导出永远不会产出的字节，那正好把"根本没有第二份实现"这条护栏拆掉（倒放连这条路都没有：伸缩器和游标都只能向前）。所以停声、**并且报出来**——而"要不要出声"和"要不要报"必须是**同一个判据的两面**（`isNormalPlayback` 一个函数），写成两个谓词漂了的形态正是"静音了但界面没说"。**三、元素倍率 = 变速倍率 × 片段速度 × 定格与否，负数取 0**（浏览器没有负的 `playbackRate`，倒放退化成逐帧 seek，那时 3 帧漂移容差**必须让路**）。这一条**顺带还了 D48 的一笔债**：定格片段在**播放中**并不定格——元素照常以 1× 走而期望位置恒定，漂移纠正每 3 个源片帧把它拽回来一次；注入前后同素材实测游走 **3.03 源片帧 / 10 次 seek / 4 个画面读数** → **0.0000 秒 / 0 次 seek / 1 个读数**。`Timeline` / EDL / 导出层 / 音频层**一行没改**。单测 1031 → **1047**（新增 16 项，文件 33 → 34），预览一致性 26/26 与多片段一致性 62/62 都重跑过。8 轮纯函数注入 + 1 轮浏览器侧注入全红，其中一轮证明那条"耦合"断言对**同源判据坏掉**免疫、只对"另写一个谓词"有牙；两次量法教训见 D49（按键和取样必须同一次调用、两点取样量不出 ±3 帧的游走）。

在此之前：**定格帧已落地**（2026-07-30，决策 **D48**）：`MediaClip.freeze?: true`——整段只画 `sourceIn` 那一帧。核心决定是**只加一个字段，"定哪一帧"由 `sourceIn` 自己回答**：加个 `freezeAt` 就是第二个真值来源，错了不报错、只表现成"定格定在了别的地方"。代价是「定格」这个动作要改 `sourceIn`（`freezeClipAt` 把它挪到播放头那一帧），于是**播放头不在片段里时拒绝、不夹到最近一端**。派生行为全部收在既有函数里：`sourceMicrosAt` 恒定分支（**判在速度之前**，否则倍率乘的是一个恒为 0 的量，靠"另一个因子恰好是 0"蒙对）、`clipSourceFrames` 返回 1（于是**出点想拉多长都行**）、`availableHandle` 返回 `Infinity`（定格片段**永不定格边缘帧**，不给这条会报出一个不存在的帧数）、裁入点与切分右半段**不推 `sourceIn`**。反过来**解除定格必须当场校验素材够不够长**——定格期间片段能拉到任意长，不校验就是尾部那一层画面静默消失。和速度**互斥用拒绝不用静默无效**（D19 那类最坏的一档），但**字段保留**、界面上那一节整个不出现（D40 的先例）。**只作用在画面上**：音频轨拒绝 + 归一化兜底，因为那边的失效形态**不是"没效果"而是静音**（`mix-plan` 会算出零长的源片区间）。**导出层和音频层一行没改**——reader 的"只能向前"对恒定时刻是非递减，`advance` 里那个循环天然停住，这是 D39 那条"取帧决策只能来自 `sampling.ts`"第二次付息。单测 1007 → **1031**，多片段一致性自检 54 → **62**。8 轮纯函数注入 + 1 轮导出侧注入全红且都红在对的断言上，其中导出那一轮推着我把自检的两个探针隔远（+5 帧的漂移被 20° 容差整个吸收）；手验里两次栽在量法上，见 D48。

在此之前：**轨道增删与素材删除已落地**（2026-07-30，决策 **D47**）：`addTrack` / `removeTrack` / `removeSource` 三个纯函数，都进撤销栈。价值同 D43——**让界面上摆着但走不通的路可达**（那个「+」从 M1 起一直 `disabled`，而素材根本删不掉）。三条纪律：**轨道 id 用可读形式且从当前时间轴现算**（`V3` / `A3`，取同前缀最大号 + 1；这不违反 D36/D41 那条禁令——那两个坑坏在模块级计数器随页面加载重置、记不住快照用掉的号，而时间轴本身就是真值来源），**取最大号 + 1 而不是最小空号**（剪贴板按 `trackId` 记原轨道，复用刚删掉的号会把片段粘进一条不相干的新轨），**删素材是全体或拒绝**（引用片段有一个在锁定轨道上就整批拦下——"素材没了、片段还留着"不是部分成功而是非法状态，`resolveSource()` 当场抛、预览崩，正是 D23 那条守着的东西）。**只动 Timeline 不碰资产库**：字节交给"孤儿 + 够老"的清理（D37 第 3 刀，它的判据必须跨项目算），顺带保住撤销。引用判据一律问 `clipSourceId()`（图片片段同样带 `sourceId`）。单测 988 → **1007**。顺带纠正一条清点错误：**空格播放 / 暂停早就有**（D26 那轮就在 `Preview.tsx` 里，因为播放状态归它持有），交接文档把它列成缺口是错的，这一刀没为它写一行代码。

在此之前：**画面裁剪已落地**（2026-07-30，决策 **D46**）：`ClipBase.crop?: CropInsets`——从源片四边各切掉多少，**按源片尺寸的比例存**（分辨率会换，D37 的指认页允许重导一版 720p，按像素存就静默裁掉另一块画面）。几何收在 `cropRect()` 一处，顺序定死**裁剪 → 留边 → 变换**：**留边按裁剪之后的宽高比算**，可见后果是留边方向会翻转（横屏素材左右裁窄之后黑边从上下跑到左右），两个自检都拿这个翻转当"裁剪确实生效"的判据。**不是可动画属性、不进批量面板**（范围决定，加是纯加法）。顺带把"这一层长什么样"那三行收成 `sampling.ts` 的 `layerLooks()`——它有两个调用点（导出 / 预览），加第四个字段漏掉一边就是"预览里裁过、成片里没裁"。单测 966 → **988**，Pixi spike 31 → **34**，预览一致性 23 → **26**。这一刀的三处返工全是**量法**而不是被测对象：写死的期望色相错了 7°（被容差吸收）、整幅平均色把左右黑边算进去（读数等比暗 9%）、注释里"步骤顺序有意义"的理由被注入实验推翻。

在此之前：**批量改属性已落地**（2026-07-30，决策 **D45**）：多选时检查器不再只报个数，变换六项 / 调色四项 / 音量一项都摊开可改。核心是**"多个值"这个读数不能折叠成其中一个**——挑一个片段的值显示，用户改一下之后 N 个真的都变成了那个值，**这个谎自己变成了真的**，事后完全查不出来。失败语义**照抄 D42 的判据**并落在"各自独立"这一类，于是 `removeClips` 那条循环抽成 `perClip()` 两家共用，而它**必须分三类不是两类**（"值没变"算进 skipped 就会报出"5 个里有 2 个没改"而那两个本来就是对的）。`propertyApplies()` 同时成了单选那两条 JSX 门的唯一判据。**有动画的片段跳过并按属性点名**（批量写的是静态值，那时画面上生效的是关键帧），**重置刻意不跳过**（它的语义就是"改静态值、不动关键帧"）。浏览器里抓到一个 typecheck 和单测都碰不到的 bug：草稿判据遇上"没有共同值"被写成了作废草稿，于是有片段在锁定轨道上时**多位数打不进去**（敲 70 得到 0，不报错）——把一个折叠过的读数当成了"我的写入生效了没有"，同 D44 那个夹紧振荡是一族。单测 939 → **966**。

在此之前：**轨道开关与右键菜单已落地**（2026-07-30，决策 **D43** / **D44**）：轨道头那三个按钮（锁定 / 静音 / 隐藏）通电，片段上右键出菜单。**这一刀的价值不在新能力，而在让已经写好的路径可达**——模型和管线早就尊重那三个字段，只有开关缺着；而 `locked` 从界面上原来造不出来，于是 `removeClips` 那条"部分成功"分支在产品里不可达。两条纪律：**开关必须进撤销栈**（字段在 `Timeline` 里、历史存的是整份快照，绕过去的话之后任何一次撤销都会把它连带回滚），**`locked` 不能被"轨道已锁定"挡住**（否则锁上就再也解不开，这是唯一一处例外）。右键菜单那半在浏览器里抓到一个 typecheck 和单测都碰不到的 bug：夹进视口写成了**振荡**（读已经挪过的矩形、比原始坐标），React 报 `Maximum update depth exceeded` 组件直接崩；改成不动点之后又发现右边缘差 6px，因为 `position: fixed` 的宽度是 shrink-to-fit 的、贴边时量到的是被挤过的宽度（加 `width: max-content`）。单测 926 → **939**。

在此之前：**多选已落地**（2026-07-30，决策 **D42**）：选中从一个 id 变成 `selectedClipIds`（数组、无重复、不指向已不存在的片段），⌘ 点选加减 / ⌘A 全选 / Esc 清空，批量删除 / 复制 / 粘贴 / 副本 / **整组拖拽**都是**一条撤销**。核心是**批量的失败语义分两类，判据是"部分成功是不是一个用户没要的新状态"**：删除各自独立（逐个做、做不成的报出来），移动 / 粘贴 / 副本是整体的（**全体或拒绝**）。而"部分成功"**不能走 `reason`**——`apply()` 在 `changed:true` 时不看它，写在那里就是静默丢掉，所以 `BatchResult` 多一个 `skippedReason` 由 store 在 apply **之后**再报一次。三条藏在细节里的：**移动中的片段之间不算重叠**（漏掉就是"紧邻的片段多选后拖不动"，磁吸候选同样要排掉整组，`snapTargets` 的参数因此从一个 id 改成一组）、**夹紧是调整整组位移而不是各自压到 0**（后者把越界的压成一叠）、**副本落点是"整组往后平移组的跨度"**（各自取出点会让相邻几个按 ⌘D 永远失败，而单个时退化成 D41 的"紧接着它"）。**整组拖拽不换轨**、**检查器多选时只报计数**（后半条**已由 D45 取代**，但"不许挑其中一个的值显示"那条禁令仍然成立），理由都在 D42。**框选**当天作为第 2 刀补上（判据在纯函数 `clipsInBox`，交互在 `use-marquee.ts`）。单测 876 → **926**。八轮反向验证里**一轮证明我注入的东西根本不改变行为**（`copyClips` 的排序对粘贴锚点无关，锚点是 `Math.min`），于是改的是注释里那个假理由，并补了一条"⌘ 点选顺序倒过来落点不变"的用例——它要**两个改动一起**才红。

在此之前：**片段复制 / 粘贴 / 副本已落地**（2026-07-30，决策 **D41**）：⌘C / ⌘V / ⌘D，编辑逻辑在 `operations.ts`、剪贴板是 store 上一个字段（**不进撤销栈、不进快照、切项目清空**）。落点纪律是**粘回原轨的播放头处、放不下就拒绝**（不自己找轨道、不波纹推开），副本落在**原片段出点**且**不动剪贴板**。顺手修掉一个从 M1 就在的 bug：`addTextClip` 的片段 id 用的是模块级计数器，和 D36 记的素材 id 是同一条纪律，只是更坏——打开存过的项目再新建一个文字片段就撞 id，而 `replaceClip` 按 id 映射会把两个一起改，全程不报错。现在片段 id 走 `newClipId()`。

在此之前：**M6「保音高变速」全部完成**（2026-07-30，决策 **D40**）：算法本体、接进混音、界面三刀。`audio/time-stretch.ts` 是 WSOLA，形状上的关键决定：**它不是纯函数，是"只能向前推进 + 带输出侧回看队列"的对象**，锚点是片段音频起点。理由是**累积对齐链消不掉**（第 k 块放哪取决于第 k−1 块实际落在哪），三条剪链方案的失败形态都记在文件头和 D40；而这个仓库里已经有一个同形状的宿主——`ClipAudioCursor`。于是"分段与不分段逐样本一致"是**结构性**成立的，不靠运气。原速走**直通**（一个样本都不动），倒放报错，相似度在**声道混合**上算一次、所有声道共用一个偏移。**21 项单测**（820 → **841**），四轮反向验证里**两轮改的是断言不是代码**：探针频率 1000Hz 让失配相位恒等于零（失配是 `2π·f·hop/rate`，在 hop=720/48kHz 上正好 15.0，关掉整个对齐搜索照样全绿），以及判据原本量"相邻样本最大跳变"而**重叠相加天生不产生单样本跳变**（失配表现为幅度周期性塌陷，要量逐窗 RMS 的最小/最大之比）。**第 2 刀（接进混音）也已落地**：`MediaClip.preservePitch?: boolean`（缺省 = 重采样，**不动 `SNAPSHOT_VERSION`**）→ `clipPreservesPitch()` → `AudioJob.stretch` → 每个片段一个伸缩器、跟着 `ClipCursorPool` **成对借还**，`playbackRate` 留在 1。排期层刻意给**两个坐标**（片段本地区间 + 导出原点）而不是一个和值：起播时刻必须落在**整数输出样本**上，而 `whenSeconds` 是亚采样精确的。M0 加了 5 条（78 → **83**），判据是**提示音自己的频率**——保音高段 **1000Hz**、只差那个开关的对照段 **2000Hz**（后者同时证明这个量法量得到音高）；分段自检加了第三组「保音高 2×」，**12 段与单段逐样本一致到 1.82e-12**，那是跨段对齐链的端到端护栏。四轮反向验证全红，其中一轮让我**补了一条断言**：坐标系混用原本只被间隔断言间接抓到（窗口里数不够声数，最大偏差却只有 0.4ms），补一条按 D35 口径的位置断言之后才红在正确的理由上（偏 **1027ms** = 整整一个片段起点）。**第 3 刀（界面）**：检查器速度那一节加一行「声音」的下拉（不是勾选框——勾选框的未勾状态会读成"默认/中性"，而它的实际含义是"声音会变调"，那个结果得被指名），提示变成随选择变的两句话；**原速下和无音轨素材上整行都不出现**（那时选择不产生任何效果，摆个不起作用的控件同样是说假话），但**字段不跟着清掉**。**预览一行没改**——它跑的就是同一个 `createMixer`（D26）。

在此之前：**M5「变速」全部完成**（2026-07-29/30，决策 **D39**）：**模型 + 取帧 + 编辑入口**、**音频**、**界面**三刀。`MediaClip.speed?: Rational`（缺省 = 1×，改回 1× 要把字段整个删掉，因为取帧对原速有一条"不乘不除"的原路径）；换算收在 `frameToMicrosScaled()` 一处、**只取整一次**（先 `frameToMicros` 再乘倍率是取整两次，实测 1.5× 下相邻帧步长在 50000/50001 之间交替）；`clipSourceFrames()` 是"消耗多少源片帧"的唯一答案，裁出点、转场余量、定格帧数都问它。**画面侧端到端就对、导出层一行没改**——reader 全程走 `renderSourceMicros`，那条"取帧决策只能来自 `sampling.ts`"的纪律在这里付了息。**声音也跟上了**（第 2 刀）：`AudioJob.speed` → `AudioBufferSourceNode.playbackRate`，**原速时那个属性一个字都不碰**；游标回看队列要乘最大速度（漏乘就是 D22 那个「除第一段外全部静音」）。代价是**声音跟着变调**（这就是重采样），保音高是另一刀，界面上要说出来。M0 加了 5 条：判据是**相邻两声的间隔**（源片每秒一声，2× 下实测 500/500/500ms、偏差 **0.0ms**），同一次导出里放一个 **1× 对照段**（1000/1000/1000ms）证明差别来自速度。**第 3 刀（界面）**：检查器里一节速度（6 个预设 + 百分比输入 + 片段新长度），**变调提示只在"速度不是 1× 且这个素材真有音轨"时出现**（给无声画面挂"声音会变调"是句假话）；波形和缩略图铺开的是**源片**区间，所以要乘速度（`clipSourceFrames` / `speedFactor`），漏乘的表现是"波形只画出前一半然后停住"而两者各自看都像对的。`SNAPSHOT_VERSION` **刻意没有 +1**，理由见 `project-snapshot.ts`：纯加法的可选字段不属于"老记录读出来是坏的"那一类，为它 +1 会把用户现有项目全部变成不可见。

**回归门禁已落地**（2026-07-29，**D38**）：`pnpm gate`（typecheck + 单测 + build）上了 GitHub Actions。「主 chunk 不许有 mediabunny / Pixi」由 `vite.config.ts` 的 `bundleGuard` 执行，判据是**模块图**——`grep -c mediabunny` 是恒为真的假断言（压缩丢包名，实测在 mediabunny 本体那个 chunk 上也返回 0），而它当过一次验收证据。**门禁绿不等于成片是对的**，四个浏览器自检不在里面。

在此之前：**M4 全部完成**（2026-07-29）。**第 3 项「项目管理与首页」第 1 刀**（界面稿 [design/kerf-home-mockup.html](design/kerf-home-mockup.html) v3，决策 **D37**）：**多项目存储 + 首页 + 自动存盘生命周期**。`meta` 从单键 `current` 换成按项目 id 存（`SNAPSHOT_VERSION` 4 → 5，旧记录版本不认、自动不可见），`Timeline` 加 `name` + `namedByUser`（自动取名只做一次、重命名后不再自动，靠标志不靠猜），`assets` 记写入时间（给第 3 刀"孤儿 + 够老"的清理用；**旧记录是裸值，清理遇到没时间戳的必须当"这一轮先别删"并回填，绝不能当"很老可以删"**），默认落首页（卡片：封面按需抽第一个视频片段的首帧、离线标记由 `isReadable` 惰性填，两个读数刻意分开）。**"崩溃恢复"这个概念随项目化溶解掉了**——快照就是项目本体，"上次崩没崩"没有判据，那条恢复提示已整个删掉。

**第 2 刀「指认页」也已落地**（2026-07-29）：打开项目拆成 `inspectProject()`（**只读，不装载不落盘**）和 `loadProject(id, replacements)` 两步，读不动的素材停在指认页让用户重指一份文件，**离线状态只存在于这一刻、永不进 EDL**（下游一行没改）。取舍收在纯函数 `state/reidentify.ts`：**种类不一致是拒绝**（拿 MP3 指认视频素材不是"换了一版"，是指错了文件），尺寸 / 帧率 / 时长不一致**只警告不阻止**；**指认到的文件，元数据必须跟着换、只保 `id`**（只换文件会留下陈旧的第二真值来源，静默取错帧）。

**第 3 刀「落盘失败两级提示 + 清理入口 + 存储读数」已落地，M4 到此全部完成**（2026-07-29）。`lastPersistError()` 从"零个消费者"变成两级读数：顶栏常驻红字 + **只在"正常 → 失败"沿上弹一次**的横幅（状态机是纯函数 `state/persist-status.ts`，防抖 1 秒意味着失败每秒重演，按次弹就是风暴）。失败**折叠回根因**：配额满走横幅、出路是清理；隐私模式是能力性事实，**进首页就提示**、且不给清理入口（库都打不开，清理是条走不通的出路）。文案由纯函数生成，所以"改动不会被保住"这句过头话钉得住（`flush` 写整份 timeline，清出空间后会全部补上，只有关标签才真丢）。清理判据是**孤儿 + 够老**（`state/asset-cleanup.ts`）：引用集合从**所有**项目算（漏一个项目就删一批）、**没有时间戳的旧记录必须当"这一轮先别删"并回填**（当成"很老可以删"会一次删光）、刚写入的孤儿留到下一轮（跨标签窗口）。存储读数**自己数、空态整条不出现**。

在此之前：——**纯音频素材（配乐 / 旁白）**（**D35**，2026-07-29）。此前 `probeFile` 直接抛「没有视频轨」、导入框只收 `video/*`，所以**加不了配乐**，而混流、波形、音量包络、交叉淡化早就都能用了。`MediaSource` 随之改成判别联合（`AvSource | AudioOnlySource`），帧栅格**派生不存**，导入也从"载入"改成**追加**（三件事缺一不可，理由见 D35）。

在此之前：**M2 与 M3 的计划项都已全部完成**（M3 五项均在 2026-07-28）——**分段混流**（**D22**）、**长片端到端实测**（含真机）、**崩溃恢复**（EDL 自动落 IndexedDB，**D23**）、**能力探测降级**（环境体检，**D24**）、**移动端导出限制**（做成量速度 + 预测耗时 + 提醒，不封顶，**D25**）。

前半段已完成：判别联合（**D8**）→ 图层变换（**D9**）→ 关键帧求值（**D10**）→ 接进两条路径 → 文字渲染（**D11**）→ 编辑界面（**D12**，同时还掉了 D10 那笔"裁入点要平移关键帧"的债）。之前：M1.5 导出闭环、M1.6 PixiJS 后端 spike（见 PLAN.md §7 M1.6 与 §6 的 D5）。

后半段：Pixi 后端接进预览和导出（2026-07-27，**D16**；两个前置 `recover()` 与导出复用常驻合成器见 **D15**）→ **一级调色**（亮度 / 对比度 / 饱和度 / 色相，**D17**）→ **3D LUT**（`.cube`，**D18**）→ **转场的时间模型 + 交叉溶解**（**D19**）→ **shader 转场**（擦除 / 圆形张开 / 推移，**D20**）→ **音频交叉淡化**（等功率 / 等增益，**D21**）。**M2 的功能项到此全部做完。**

**预览出声已落地**（**D26**）：预览跑同一个 `createMixer`、播导出会写进成片的那份 PCM，所以硬规则 2 在音频上的护栏比画面那边更强——不是共用算法，而是**根本没有第二份实现**。那条「预览听到的 == 成片里的」端到端自检**也已补上**（**D34**，进了 M0）：AudioWorklet 无损抓预览输出、和同一份 PCM 逐样本对拍，实测最大差 3.02e-16；补的过程中抓到两个真 bug（起播余量没进漂移判据、`await resume()` 会挂死）。**音频波形已落地**（**D29**）：音频片段上画峰值包络，音量包络画在同一张 canvas 上（绝对刻度 + 一条 100% 参考线），于是"这一段有多响 / 我把它调成了什么样"终于能在时间轴上看见。

**故障转场已落地**（**D30**）：改用 **PCG 整数哈希**（GPU 与 JS 逐位相同），于是那条 GPU-vs-CPU 断言在故障上同样有牙齿——实测 15 个用例逐通道完全相同，而把 GLSL 里的哈希常量改一位就红到 200/255。**M2 的功能项到此全部做完。**

**自定义字体已落地**（**D31**）：文字层能导入 `.ttf/.otf/.woff2`，EDL 里存字节 + 一个我们生成的族名（`KerfFont-…`），**预览和导出各自注册一次 `FontFace`**。`FontFaceSet` 每个上下文一份，漏一边的表现是"预览里是用户选的字、成片里不是"且两边都不抛错——所以纪律是**先注册、再进 EDL**，而 `rasterizeText` 对没注册过的族名**抛错**，把那个静默回退变成一次崩。实测：预览与成片的文字包围盒**逐像素相同**（656–1267），换回默认字体是 613–1311；把 Worker 里那次注册注掉，导出当场报出族名而不是悄悄换字。

**时间轴上的关键帧轨已落地**（**D32**）：选中片段的每个有动画的属性各一行，钻石可以横向拖动改时间，顺带还掉了 D10 留下的那笔债——`moveKeyframe` 让"改关键帧的时间"从"删了重打"（两步撤销）变成一步。

**M2 到此全部完成**（含界面三件小事）：最后一件**文字阴影颜色**已落地（**D33**），做法是"原生取色器 + 一个不透明度百分比"，拼装规则收在 `compose/css-color.ts` 一处。

**音量包络已落地**（**D27** 静态音量 + **D28** 包络）：`MediaClip.volume` 和转场淡化**合成一条增益链**——静态音量是常数、乘进淡化曲线的采样点；打了关键帧则由排期层把**淡化 × 包络逐帧乘成一条 `gainCurve`**，两种情形都**只穿一个 `GainNode`**，恒等值仍走"不穿节点"的原路径。音量是**第三组可动画属性**（`AUDIO_PROPERTIES`），共用那张关键帧通道表但求值分岔（`resolveVolume` 返回标量）。

M3 里 **分段混流 + 长片实测已完成**（含真机）：混音峰值 60 倍片长恒为 **5.7MB**（起点是 30 分钟 989MB），**Chrome / 桌面 Safari / iPhone 三处 30 分钟都跑通**（13.5× / 4.4× / 7.36× 实时）。曾判成"Safari 30 分钟死等"，**那个结论已撤回**——是后台标签节流叠上一个真 bug（`StreamTarget` 的攒批模式在 Safari 上写出坏文件），两件都不是内存，经过见 PLAN.md §8 风险 1。

**移动端长片这条风险已按实测通过结掉,而且"墙"一道都不存在**：iPhone 上**整条阶梯一页跑完**（30 秒 / 2 分钟 / 10 分钟 / 30 分钟 = 4 次导出连跑）**四档全过**,吞吐 6.01 / 7.40 / 7.47 / **7.41×**（越往后越快,第一档是冷启动）,混音峰值与循环峰值**四档逐字节相同**（5.7MB / 8.0MB,倍率 1.00）,泄漏全 0,每档都解回来比对了帧数和音画时长（54000 帧 / 1801.8s / 55.7MB）。**先后立过三道"墙"、全部倒了**：片长（四档全过）、导出次数（一页 24 次连导全过、吞吐死平）、跨档累积（就是这一轮）。三种失败脸相（`Decoder failure` / `decode:V1` 死等 / 标签页被杀）**没有一个是被测对象的问题**——两个出自并行自检污染 + 死等把"页面被挂起"算成停滞（都是量法 bug,已修:同页互斥 + `RunId` 认号 + 看门狗靠定时器跳间隔自证,见导出层约定）,第三个出自**手机被连续压了三小时后的降频**(同一档 30 秒从 7.3× 掉到 4.33×,凉下来就过了)。详见 PLAN.md §8 风险 4。**崩溃恢复**（D23）、**能力探测降级**（D24）、**移动端导出限制**（D25）都已落地，**M3 的计划项到此全部做完**。最后一项做成了"量出这台机器的吞吐 + 预测耗时 + 慢于实时就提醒"而**不是封顶**：依据是速度不是内存（iPhone 上 2160p 只有 0.66× 实时、1080p 有 3× 余量），而按设备分档测的不是被测对象。

```bash
pnpm dev          # 起开发服务器
pnpm dev:device   # 真机自检用：监听局域网 + 自签 HTTPS（WebCodecs/OPFS 要安全上下文，http 局域网地址不算）
                  # 加 ?autorun=m0|pixi|timeline|preview|device 会自动跑完并把完整结果 POST 回 .reports/
pnpm test         # 跑单元测试（1074 项：变速梯子 16 / 时间伸缩 23 / 素材帧栅格 19 / 图片解码尺寸 5 / 颜色字符串 15 / 字体族名 5 / 波形取桶 10 / 转场混合 37 / 预览音频排期 17 / 吞吐模型 12 / 环境体检 21 / 项目快照 43 / 素材指认 21 / 落盘读数 15 / 资产清理 20 / 时间基 29 / 状态层 441 / 取样映射 36 / 转场时间模型 30 / 淡化曲线 24 / 混音排期 39 / 混音分段 15 / 预设 14 / 合成几何 21 / 色彩矩阵 23 / LUT 22 / 关键帧 35 / 文字断行 23 / 栅格缓存 12 / 常驻量 12 / 混音 6 / 编码延迟 12）
pnpm typecheck    # 类型检查，严格模式
pnpm build        # 构建（自带主 chunk 断言，见「首屏体积」）
pnpm gate         # 门禁三件套：typecheck + test + build。CI 上跑的就是这三条
```

**`pnpm gate` 绿不等于成片是对的。** 它挡的是"改坏了但没人跑"，覆盖不到四个浏览器自检——而这个仓库里几乎每一个真 bug 都是那四个抓到的。改到下面那张表里的东西仍然要自己跑一遍。

## 状态层约定（M1 起）

- **改 Timeline 只能走 `useTimeline` 的 action**，它们内部统一经 `apply()` 进撤销栈。绕过去直接 `set({ history })` 会产生撤销不了的编辑，是最难查的一类 bug。
- **编辑逻辑写在 `src/state/operations.ts` 的纯函数里**，不要写进组件或 store。那里能脱离浏览器单测，而移动/裁切/切分的边界条件多到必须靠测试锁死。
- **同一轨道内片段永不重叠**，这是核心不变量。越界操作返回 `changed:false` + `reason`，不要静默失败。
- **`MediaSource` 也是判别联合**（`AvSource | AudioOnlySource`，**D35**）。纯音频素材（配乐 / 旁白）**没有** `fps` / `durationFrames` / `width` / `height`，不要给它填 0 或者编一个帧率——理由同下面那条（漏判一处不报错：代理会去转一个没有视频轨的文件、缩略图画出一条空白带、合成器拿到 0×0 的图层）。凡是"这条路径只处理带画面的素材"的地方就把类型收窄成 `AvSource`，别在函数体里判（`VisibleMediaClip.source`、`ActiveSource.source`、`probeAvFile`）。
- **判别联合只在字段集合真的不同的地方才帮得上忙。** 加图片那一轮，导出侧 `toLayer` 的最后一行是 `samples.get(visible.clip.id)`——`VisibleImageClip` 和 `VisibleMediaClip` **都有 `clip.id`**，所以少写图片分支照样编译得过，只会去查一个不存在的解码帧，然后返回 null、**静默少一层画面**。编译器一个字都没说。所以"改了联合就跟着编译错误走"是必要的、不是充分的：还要自己过一遍所有 `switch`/`if` 链，看有没有靠"共有字段"蒙过去的分支。
- **纯音频素材的帧栅格是派生的，不存**（`sourceGridFps()` / `sourceDurationFrames()`）。`sourceIn` 的单位不自由：**裁入点把时间轴帧号的增量直接加到 `sourceIn` 上**，所以那个栅格必须就是项目帧率。存一个"导入时的项目帧率"就成了会陈旧的第二真值来源，而陈旧的表现是"裁一帧变成裁 1.2 帧"且不报错。**这条派生的地基是"项目帧率在时间轴上有片段之后不再改动"**，由 `addSource()` 保证——它的判据是"一个片段都没有"，不是"没有画面素材"，改这一条之前先想清楚所有音频片段的入点会被重新解释。帧数用 `floor`：宁可少报一帧，也不能报出一帧解不出内容的位置。
- **变速是 `MediaClip.speed?: Rational`（缺省 1×），而"消耗多少源片帧"只有 `clipSourceFrames()` 一个答案**（**D39**）。三条地基纪律：**原速必须走"不乘不除"的原路径**（判据是 `isNormalSpeed()`，判 `=== undefined` 不够——`{num:2,den:2}` 也是原速，掉出快路径就等于让没变速的项目改变输出）；**帧 → 微秒 → 乘倍率只允许取整一次**（`frameToMicrosScaled()`；先 `frameToMicros` 再乘是取整两次，实测 30fps 1.5× 下相邻帧步长在 50000 / 50001 之间逐帧交替，画面看不出、音频上是每帧一个亚微秒相位抖动，同 `exactSeconds` 那条）；**倒放不做**，`VideoTrackReader` 的"取帧只能向前"是硬规则 3 的前提。**速度只作用于"从片段起点走了多久"，不作用于 `sourceIn`**（把入点也乘进去的表现是"裁过入点的片段一变速就跳到别处"，而入点为 0 的片段完全正常——同 D35 那个被乘以零的因子）。跟着表态的地方漏一处都不报错：**裁出点判据**（漏乘 → 2× 下能把出点拉到源片之外，尾部那一层画面静默消失）、**转场余量**（在源片侧算、报出来要 `unscaleBySpeed()` 换成时间轴帧，混着用的表现是"界面说余量够、窗口一开就定格"）、**`splitClipAt` 右半段的 `sourceIn`**（漏乘 → 右半段从中间跳回去）。**`unscaleBySpeed` 用 `floor` 而 `scaleBySpeed` 用 `round`，刻意不对称**：前者回答"余量够不够"，多算一帧就是报"够"而那一帧解不出来。**关键帧不跟着缩**（偏移相对片段起点、单位是时间轴帧，属于时间轴侧）；**速度不是可动画属性**，它不在 `AnimatableProperty` 里，所以 `setKeyframe` 编译期就传不进来——那比运行时拒绝好，而理由是速度关键帧会让源片时刻变成速度曲线的积分（另一个模型，要配曲线编辑器）。**改速度的语义是"保内容、改长度"，放不下就拒绝、不隐式波纹**（静默推走整轨是"选了 A 拿到 B"）。**音频侧只需要一件事：把倍率告给 `playbackRate`**（`AudioJob.speed`），因为源片区间已经由 `sourceMicrosAt` 缩过了；漏掉它的表现是**声音全在、泄漏为 0、不抛错，只是和画面差一倍**。但**游标的回看队列长度要乘最大速度**（`mixdown.ts` 的 `backlogSeconds`）——pad 是输出帧数而队列量的是源片秒数，漏乘就是 D22 记过的「除第一段外全部静音」；取所有 job 的最大速度而不是上限 8，因为那个队列的长度直接是内存。**声音跟着变调**是这一刀的已知取舍（重采样），保音高要单独一刀且必须有 CPU 参照实现。
- **保音高的接线只认 `AudioJob.stretch` 这一个字段，而它给的是"两个坐标"不是一个和值**（**D40**）。有这个字段就走伸缩路径，那时 **`playbackRate` 必须留在 1**（两处都乘一遍的表现是速度变成平方，而画面照旧，实测间隔从 500ms 变 250ms）。三条纪律：**要读多少源片由 `sourceRangeFor()` 说**，不是 `srcStart/EndSeconds`（后者刚好是"输出 × 倍率"，伸缩器还要左边一个搜索半径、右边一个窗，漏掉的表现是每段结尾一小段静音）；**起播时刻是 `originSeconds + 整数样本 / 采样率`**，不能用 `whenSeconds`（后者亚采样精确，而伸缩的输出栅格是整数样本，两段各自取整却不按同一把尺子对齐就是 `sampleAlignFrames` 记过的那声咔哒）；**片段本地区间要减 `clip.timelineIn`**，混用导出坐标系的表现是"整段声音偏移了一个片段起点的量"，而**片段恰好从导出区间起点开始时完全正常**（第二次踩 `sampleGainCurve` 那块地）。伸缩器和解码游标**成对借还**（`ClipCursorPool`）：只还游标会留下一条链停在旧位置的伸缩器，下次借到它当场抛"只能向前"。**回看队列开小了不会静默**——那道守卫会报出带数字的错，这一点和解码游标那条路不同（那边当年是一排 0.000）。
- **保音高的算法在 `audio/time-stretch.ts` 一处，而它不是纯函数、是"只向前 + 回看队列"的对象**（**D40**）。会写错的是**形状**不是算术：任何能听的时间伸缩都带一条**累积的对齐链**，把它写成"任何输出区间都能独立算"的纯函数做不到（三条剪链方案的失败形态见文件头），而链被段边界打断的表现是**每隔一段咔一下**、RMS 包络断言对它完全免疫。所以宿主形状抄 `ClipAudioCursor`：**一次起链、之后只向前，相邻两段重叠的那截从输出侧的回看队列给**（长度取 2×pad）。推论三条：**原速必须走直通**（`num === den`，一个样本都不动）；**倒放报错**（同硬规则 3 的前提）；**相似度只在声道混合信号上算一次、所有声道共用那个偏移**（各挑一个的表现是立体声像左右乱晃，而每一路自己听都是对的）。已知取舍：链的起点是第一次请求的位置，所以拖过播放头之后**预览与成片会差一个不足一块的相位**（内容相同、听不出来），这一条不能靠"再对齐一次"补——那就是把链接回去了。**改这个文件要先看单测里那四组断言的反向验证记录**：探针频率必须由 hop 算出来（写死 1000Hz 会让被测量恒等于零），判"对齐生效了没有"要量**逐窗 RMS 的最小/最大之比**而不是相邻样本跳变。
- **导入素材是"追加"不是"载入"**（`addSource()`）。覆盖式载入会把上一次的编辑静默扔掉（撤销栈里只留一条"导入"），而配乐这件事本身就要求先画面后音乐。画面素材落 V1（画面轨**自下而上**找第一条放得下的，和 `addTextClip` 相反——素材该进主视频轨，文字该进最上面的字幕轨），声音落 A1；带音轨的画面素材两个片段**必须同起点**，任一放不下就**整体拒绝**，不允许"画面放下了、声音挪到了别处"（那是音画错位，不是失败）。
- **图片是第三种片段（`ImageClip`），它有 `sourceId` 但没有 `sourceIn`**（**D36**）。"用哪个素材"和"用它的哪一刻"是两件事，而一张图没有后者。给它一个恒为 0 的 `sourceIn` 会让四件事跟着错：裁出点被"源片只有一帧"挡住（而拉长它正是图片最主要的编辑动作）、切分徒劳地推进它、取帧算出一个没人读的时刻、reader 拿一个 PNG 去问视频轨（拿到 null，于是**画面静默消失**）。"这个片段引用哪个素材"要问 **`clipSourceId()`**，别散着写 `kind === "media" && …`——加了图片之后那种写法会漏掉图片，而漏掉的表现是"素材丢了却留着片段"。
- **图片的"源片长度"是 `Infinity`**（`IMAGE_SOURCE_FRAMES`），因为它想占多久都行。别改成一个大整数：那会在某个片长上突然变成"到源片末尾了"而用户看不出为什么。片段的**初始**长度是另一件事（`IMAGE_DEFAULT_SECONDS`，5 秒）。
- **素材 id 不能用模块级递增计数器**（`media/source-id.ts`，D36）。计数器随页面加载重置回 0，而崩溃恢复带回来的 `sources` 里是上一次会话的 `src-1`——于是"恢复完再导入一个素材"被 `addSource()` 那道"已经在项目里了"当场拒掉。这个 bug 从 M0 就在，**D35 把导入改成追加之后才看得见**（此前每次导入都换掉整条时间轴，`sources` 永远只有一个）。用 `crypto.randomUUID()`。
- **`Clip` 是判别联合，不要给它加可选字段来"兼容两种片段"**。`sourceId` / `sourceIn` 只在 `kind:"media"` 上；`TrackKind` 只有 `video | audio`，"这一段是素材还是文字"看 `clip.kind`，不看轨道（字幕轨 T1 的 `kind` 就是 `"video"`，这是对的）。编辑操作里**只有裁切和切分**需要分岔（源片够不够长、右半段要不要推进 `sourceIn`），其余都只动时间轴占位。理由和放弃过的方案见 PLAN.md 的 **D8**。
- **剪贴板存在 store 上，而它的三条边界都是"不"**（**D41**）：**不进撤销栈**（复制什么都没改，进栈的话用户要按两次 ⌘Z 才回到上一次真编辑，同播放头和选中）、**不进快照**（它引用的 `sourceId` 下次打开时可能已经不在，而快照里一个引用不到素材的片段会让 `resolveSource()` 抛错，D23）、**切项目要清空**（跨项目粘贴够得到）。清空是体验，`pasteClip` 里那道"素材必须在当前项目里"才是契约——纯函数单独拿出来用也必须安全。落点纪律：**粘回原轨的播放头处，放不下就拒绝并报出挡路的是谁**，不自己找别的轨道也不波纹推开（同 `setClipSpeed`）；**副本落在原片段的出点**（按播放头放必然和自己重叠）且**不动剪贴板**（⌘D 不该把 ⌘C 的东西冲掉）。克隆时两件事漏一件都不报错：**换新 id**、**把 `transitionIn` 整个删掉**——后者的判据是"粘在某个片段**紧后面**"，落点放远的用例没有牙齿（归一化会替你清掉），而 ⌘D 的落点永远是紧后面。
- **新片段 id 走 `newClipId()`，不许用模块级计数器**（`media/source-id.ts`，D41）。和 D36 那条素材 id 是同一条纪律，长在片段上而且**更坏**：计数器随页面加载重置，而快照里带着上一次会话的 `text-1`，于是打开存过的项目再新建一个文字片段就撞 id——而 `replaceClip` 是**按 id 映射**的，改一个片段的样式会把两个一起改，`findClip` 只返回先遇到的那个。全程不报错。素材片段的 id 不走这里（`addSource` 用 `${source.id}-v` / `-a` 派生，源 id 本身已是 UUID）。**`clipSourceId()` 返回 `null` 不是 `undefined`**：判 `!== undefined` 会让文字片段的 `null` 通过门再去找一个 id 为 null 的素材，表现是**文字片段永远粘不了**。
- **选中是一组 id（`selectedClipIds`），而批量操作的失败语义分两类**（**D42**）。判据是**部分成功是不是一个用户没要的新状态**：**删除各自独立**（删掉 3 个和删掉 5 个都只是"这些不在了"），所以 `removeClips` 逐个做、做不成的报出来——整批拒绝会让"选中的一堆里有一个在锁定轨道上"变成什么都删不掉；**移动 / 粘贴 / 副本是整体的**（N 个片段的相对位置就是内容本身），所以 `moveClips` / `pasteClips` / `duplicateClips` **全体或拒绝**，失败时返回**原**时间轴（同 `addSource` 那条"不允许画面放下了、声音挪到了别处"）。**"部分成功"这个结果不能走 `reason`**：`apply()` 在 `changed:true` 时根本不看它，写在那里等于静默丢掉（用户看到 4 个消失一个还在而软件一个字都没说），所以 `BatchResult` 有单独的 `skippedReason`，store 在 `apply` **之后**再报一次——顺序反了就是自己擦掉自己。三条具体纪律：**碰撞检查要排掉整个移动集合**（整组右移时第一个的新位置压在第二个的**原**位置上，而第二个也在移动；漏掉的表现是"紧邻的片段多选之后根本拖不动"），**夹紧是调整整组的位移不是把每个各自夹到 0**（后者把越界的压成一叠，而这正是"相对位置就是内容"被破坏的形态；推论：整组已贴着 0 还往左拖时位移是 0，要算"值没变"而不是照常提交），**副本的落点是"整组往后平移组的跨度"**（各自取自己的出点会让 A 的副本落在 B 头上，于是选中相邻几个按 ⌘D 永远失败；单个片段时跨度就是它的长度，落点退化成 D41 的"紧接着它"）。**粘贴的锚点必须是 `Math.min` 不能写成 `entries[0]`**——后者只在 `copyClips` 恰好排过序时才对，而那是另一个函数的实现细节（反向验证过：单独改锚点全绿，锚点和排序一起改才红，两条纪律是共同承重的）。
- **`snapTargets` 排除的是一组 id，不是一个**（D42）。整组拖拽时同伴也在移动，吸到它们的**原**位置是错的（表现是整组拖起来一顿一顿的）。参数写成数组而不是"一个 id 或一组"的联合，为的是让漏改的调用点在**编译期**就红。
- **"恰好选中一个"的判据收在 `soleSelectedClipId()` 一处**（D42），0 个和多个都返回 null。检查器、关键帧轨、状态栏都只在"就一个"时才有意义——多选时显示其中一个的属性，用户改一下会以为 N 个都变了，而改完只有那一个变（硬规则 10 的形状）。让各个组件自己写 `length === 1 ? ids[0] : null` 就一定会漏一处，而漏掉的表现就是上面那句假话。
- **入点 / 出点标记是 `Timeline` 上两个可选字段（`markIn` / `markOut`），所以它没得选，必须进撤销栈**（**D50**）。同 D43 那条：绕过 `apply()` 之后任何一次撤销都会把标记连带回滚。而它**本来就该是编辑**——导出范围会改变成片，这正是 D43 判"静音 / 隐藏算编辑"的那把尺子；放进 store（像播放头和选中那样）意味着刷新之后标记就没了，而"圈好一段明天接着导"是这个功能的主要用法。**"标记指哪一段"只有 `markedRange()` 一个答案**：两端各自可以单独存在（只有入点 = 到末尾、只有出点 = 从头开始），这条补全规则散写在标尺 / 读数 / 导出面板三处必然会漂，漂了的表现是"导出面板说 6 秒、工具栏读数说 8 秒"，两边都不报错。**合并键按端给**（`mark:in` / `mark:out`）：连打合成一条撤销，但**两端不能共用一个键**——合并只发生在紧邻的上一条上，共用会让"打入点紧接着打出点"合成一条，⌘Z 一下把两端一起撤掉。**越过另一端时拒绝，不夹紧也不顺手清掉对面**（同 D46），拒绝原因里要印出另一端在第几帧。**时间轴变短时标记跟着夹**（`clampMarks` 挂在 `replaceTracks` 这个唯一出口上），**夹完站不住就把两端整个清掉**——不夹的表现不是报错而是多渲染一段黑画面（而 D25 那个耗时预测和空间预估都按区间算，于是一起算大），而留一个"入点等于出点"的标记会让导出面板出现一个选了就报错的选项。
- **"在播放头做某事"的动作要在 store 里读播放头，不从调用方传进来**（**D50**，`markAtPlayhead` / `freezeAtPlayhead` / `splitAtPlayhead`）。键盘处理器里那个 `playhead` 来自 React 闭包，**比 store 慢一拍**：浏览器里实测过，`setPlayhead(60)` 之后立刻按 I 打出来的是**第 0 帧**（单测碰不到，因为测试是顺序调 store 动作）。收进 store 之后这一类整个消失；传参数的写法把同一个错误留在每一个调用点上。纯函数那一层仍然收 `frame`（`setMark(timeline, edge, frame|null)`），那是给将来"拖标尺上的爪"留的口子。
- **播放头和选中不进撤销栈**；但撤销后要**逐个过滤**掉指向已不存在片段的选中（**不是整体清空**，D42）——整体清空会让"撤销一次切分"把另外几个还在的片段也一起取消选中，用户接下来那一下操作落到空处。
- **定格只加一个字段，而"定哪一帧"就是 `sourceIn`**（`MediaClip.freeze?: true`，**D48**）。加一个 `freezeAt` 会造出第二个真值来源（"两个都设了听谁的"），错了不报错、只表现成"定格定在了别的地方"。代价是「定格」这个动作要**改 `sourceIn`**（`freezeClipAt` 挪到播放头那一帧，换算和裁入点共用 `scaleBySpeed`），因此**播放头不在片段里时拒绝、不夹到最近的一端**（夹紧等于替用户改了目标）。派生行为**一处都不许另写**：`sourceMicrosAt` 走恒定分支且**判在速度之前**（否则倍率乘的是一个恒为 0 的量，看着对其实靠"另一个因子恰好是 0"蒙对，同 D35）、`clipSourceFrames` 返回 **1**（于是出点不受源片长度限制、`availableHandle` 那条余量也跟着无穷）、**裁入点和切分右半段都不推 `sourceIn`**（推了就是"拖一下片段的头，定住的画面换了一张"）。**只有画面轨上的素材片段能定格**：音频轨上存下去的后果**不是"没效果"而是静音**（`mix-plan` 拿恒定的 `sourceMicrosAt` 算出 `srcStart === srcEnd` 的零长区间，不报错），所以编辑入口拒绝之外，归一化里还有一道兜底（同 `dropOrphanTransitions`）。
- **定格片段的转场余量是 `Infinity`，解除定格反过来必须校验素材长度**（**D48**）。前者：窗口往入点之前 / 出点之后借的每一帧取帧都还是同一帧，所以定格片段**永不定格边缘帧**——不给这条分支，一个定在源片第 0 帧的片段会被算成"入场侧一帧余量都没有"，检查器于是报出一个**不存在的**定格帧数（`frozenFrames` 这个同名概念还会让人读混）。后者：定格期间 `clipSourceFrames` 是 1，用户可以把片段拉到任意长，解除之后它要真的消耗那么多帧——不校验的表现是**尾部那一层画面静默消失**，所以 `unfreezeClip` 报出"要 N 帧、只剩 M 帧"。
- **定格和速度互斥要靠拒绝，不能靠"存了不生效"**（**D48**）。`setClipSpeed` 对定格片段当场拒绝；界面上速度那一节在定格时**整个不出现**，但**字段保留**（同 D40 那条"原速下不显示「声音」而字段不清"）——解除之后 2× 会立刻回来，所以检查器要把这件事**说出来**，否则用户遇到的是"解除定格之后它怎么快了一倍"。
- **轨道 id 是可读的（`V3` / `A3`）而且从当前时间轴现算，取"同前缀最大号 + 1"**（`addTrack`，**D47**）。可读是因为它显示在轨道头和状态栏上（片段 id 不显示，所以那边走 UUID）；而"从时间轴现算"正是它不违反 D36/D41 那条禁令的原因——那两个坑坏在**模块级计数器随页面加载重置**、记不住快照里已经用掉的号，这里每次都拿时间轴判重，恢复回来的轨道天然参与。**不取最小空号**：剪贴板按 `trackId` 记原轨道（D41），复用刚删掉的号会让旧剪贴板把片段粘进一条毫不相干的新轨。**画面轨插在数组最前**（那就是 z 序最上层，`videoTracksInDrawOrder` 反转这个数组）、音频轨加在最后；**不给 `label`**——默认那五条的标签描述的是角色（主视频 / 人声），新轨道没有角色，编一个「画面 4」不比 id 多说任何东西。
- **删素材是全体或拒绝，而且只动 Timeline 不碰资产库**（`removeSource`，**D47**）。引用它的片段有一个在锁定轨道上就整批拦下：**"素材没了、片段还留着"不是部分成功，是非法状态**——`resolveSource()` 对着引用不到素材的片段直接抛、预览当场崩，而那正是 D23 那条"素材找不回来时片段必须移除"守着的东西，这里不能亲手造一个出来（判据仍是 D42 的"部分成功是不是一个用户没要的新状态"）。字节的去留归"孤儿 + 够老"的清理（D37 第 3 刀，它的引用集合必须跨项目算），顺带保住撤销——⌘Z 之后片段和素材原样回来、文件还在库里。**引用判据问 `clipSourceId()`**，散写 `kind === "media"` 会漏掉图片片段（表现是"素材删了、图片片段还在"，渲染时那一层静默消失）。删片段之后那条轨要过 `withClips` 归一化，否则后继片段留下一个指向不存在交界的转场。
- **删轨道被锁定挡住，而 D43 那条例外只属于 `locked` 开关本身**（`removeTrack`，**D47**）：锁定的语义就是"编辑会被拒绝"，而删掉整条轨是最大的一次编辑。**刻意不设"至少留一条轨"的下限**——删空之后导入和新建文字会带原因拒绝（"没有画面轨"），出路就是旁边的新建轨道；设下限反而要在两处维护"最后一条不能删"这件事。**轨道上有片段时不在纯函数里拦**，删除走撤销栈、一步就回得来，"要不要确认"是界面的事（菜单标签写明连带几个片段）。
- 连续拖拽必须传带对象标识的合并键（`move:${clipId}`），否则用户要按几十次 ⌘Z。
- **变换 / 调色 / 关键帧 / 文字的编辑入口也在 `operations.ts`**（`setClipTransform` / `setClipColor` / `setKeyframe` / `removeKeyframe` / `clearKeyframes` / `setTextContent` / `setTextStyle` / `addTextClip`），界面只做换算和渲染。夹紧范围与缺省值只有 `PROPERTY_RANGES` 一份，**UI 从这里取 min/max**——两处规则不一致时用户看到的是"输入框允许、松手弹回"。理由见 PLAN.md 的 **D12**。
- **可动画属性分三组、共用一张关键帧通道表**（`TRANSFORM_PROPERTIES` 六项 → `LayerTransform`，`COLOR_PROPERTIES` 五项 → `ColorAdjust`，`AUDIO_PROPERTIES` 一项 → `MediaClip.volume`）。打点 / 删点 / 平移 / 撤销与"作用到摆位、颜色还是声音"无关，分三张表等于全写三遍；但**求值必须分岔**（`resolveTransform` / `resolveColor` / `resolveVolume`）——把 `brightness` 混进 `LayerTransform` 不会报错，合成器不认识那个字段，表现只是"调了色画面没变"。
- **第三组的静态值是片段上的一个标量，不是一组对象**，所以 `resolveVolume` 不能复用 `resolveGroup`，而"某个属性的静态值存在哪儿"必须收在 **`staticValueOf()` 一处**：检查器要显示它、`clearKeyframes` 要往回烘、将来的曲线编辑器也要读它，三处各写一遍 `isColorProperty ? … : …` 就一定会在加第三组时漏掉一处，而漏掉的表现是"关掉动画之后这个属性的值丢了"或"输入框显示缺省值而声音不是"。都不报错。`clearKeyframes` 的烘焙同样是三分岔，音量那一支还要自己夹紧 + 判缺省（没有"一组"可以归一化）。
- **关键帧通道挂在 `ClipBase` 上，所以类型拦不住"给文字片段打一条音量曲线"**，`setKeyframe` 里显式拒掉。那条曲线永远不会被求值（文字片段没有音轨），留着就是"打了点没反应"。
- **`EditResult` 有第三种结果："值没变"**——`changed:false` 且**不给 `reason`**。滑块拖到边界后会一直发同一个值，当失败处理会让状态栏持续闪红字。store 的 `apply()` 只在有 `reason` 时才提示。
- **改回缺省值要把 `transform` 字段整个删掉，删光关键帧要把 `keyframes` 字段整个删掉**，不要留 `{x:0}` 或 `{}`。合成器判的是值不是字段，所以这不是正确性问题；但"这个片段动过变换 / 有没有动画"要能在数据层一眼看出来。`exactOptionalPropertyTypes` 下清除必须真的 `delete`，赋 `undefined` 是另一种类型。
- **转场种类分画面组和声音组，由轨道种类决定哪一组合法**（`transitionFitsTrack`）。`dissolve/wipe/iris/slide` 混的是像素，音频轨上没有像素；让用户在音频轨上选「圆形张开」再静默按交叉淡化渲染，就是硬规则 10 的"选了 A 拿到 B"。校验两道——`setTransition` 挡编辑入口、`dropOrphanTransitions` 兜底（片段不能跨轨道种类拖，所以第二道实际拦不到东西，留着是防将来新的编辑操作忘了校验）。加新种类时 `TRANSITION_LABELS` 这个 `Record` 会编译报错，那是刻意的。理由见 PLAN.md 的 **D21**。
- **转场挂在入场片段上（`Clip.transitionIn`），相邻关系不由类型保证，靠每次编辑后归一化**（`withClips` 里的 `dropOrphanTransitions`）。片段被拖开 / 前驱被删或被裁短之后那个字段就指向一个不存在的交界，留着是"界面显示有转场、画面上没有"的状态。只在**相邻关系断掉**时清，不因为"片段太短、窗口解不出来"而清——后者拉长就回来了。**切分时右半段必须显式清掉**，否则按一下 ⌘K 就凭空多一个自己没加过的溶解，还刚好在新切点上。理由见 PLAN.md 的 **D19**。
- **项目存储的取舍写在纯函数 `state/project-snapshot.ts` 里，IndexedDB 那层只存取**（`project-store.ts`；M3 做崩溃恢复时立的，M4 项目化后快照就是项目本体、规则原样成立）。会写错的是取舍：`File` 要从 EDL 里剥出去（快照每次编辑都重写，素材一辈子只写一次——混在一起就是拖一下片段重写几百 MB）、素材找不回来时**片段必须移除**（`resolveSource()` 找不到就抛，留着预览整个崩）、LUT 找不回来时**片段必须保留**只清 `lutId`（它照样渲染），字体找不回来时同样保留片段但**必须清掉 `style.fontFamily`**（留着渲染就抛，见合成层那条），三者都要**报给用户**。**多条"摘引用"的规则要一条条都过，不能命中一条就 `continue`**——`lutId` 挂在 `ClipBase` 上，文字片段同样可以有，先判的那条会把另一条挡掉而且不报错。删了片段之后要过 `withNormalizedTracks`——前驱被移除会留下指向不存在交界的转场。**不存撤销栈**：栈里每一条都引用着可能已经不在的素材。理由和放弃过的四个方案见 PLAN.md 的 **D23**。
- **`Omit<联合, K>` 会把联合塌成"共有字段"，快照里的 `SourceMeta` 必须用分配式 Omit**（D35）。`MediaSource` 改成判别联合之后，`Omit<MediaSource, "file">` 得到的只剩 id / name / kind / hasAudio / audioCodec——帧率、尺寸、时长**全部消失**。存的时候不报错（多余字段照样写进 IndexedDB），只有读回来拼 `{...meta, file}` 时才编译不过；真正坏的形态是它**编译得过**的那一天，快照少一半字段而恢复出来的素材没有时长。写成 `T extends unknown ? Omit<T, K> : never`。**改了 EDL 的形状就要给 `SNAPSHOT_VERSION` +1**（D35 那轮 2 → 3，D37 项目化 4 → 5）。
- **落盘失败要两级读数，而且显眼度是选出来的**（`state/persist-status.ts`，D37 第 3 刀）。顶栏红字是**常驻读数**（失败是持续状态），横幅只在**"正常 → 失败"的沿**上弹一次——防抖 1 秒意味着失败每秒重演，按次弹就是风暴（同 D34 那条"给错误加限流会把风暴变成周期性抽动"）。状态机是 2×2（正常/失败 × 弹过/没弹）且**必须是两个字段**：合成一个布尔的话，用户关掉的横幅会在 1 秒后原地复活。写成功一次整个复位，**再失败算新的沿**。**文案由纯函数生成、不散在组件里**，因为有一句话钉不住就完蛋：**不许说"改动不会被保住"**——`flush` 写的是整份 timeline 不是增量，失败期间的编辑都在内存里，清出空间后下一次写全部补上，只有关掉标签才真丢；说成永久丢失，用户会慌着关页面重开，**那才是真丢**。失败还要**折叠回根因**（D24）：配额满是运行时的、出路是清理；隐私模式是能力性事实、**进首页就说**且**不给清理入口**（库都打不开，那是条走不通的出路）。配套推论：清理完要 `retry()` 一次落盘，否则失败那次已经把欠账清掉、此后没有新编辑就不再写，横幅上那句"清出空间后会自动接着保存"在用户眼里就是假话。
- **清理无引用资产的判据是"孤儿 + 够老"，不是"没人引用"**（`state/asset-cleanup.ts`，D37 第 3 刀）。只看引用**在一个标签里成立，两个标签就不成立**：标签 A 刚导入素材（`assets` 已写、快照还没写），标签 B 回首页点清理就把它删了，而误删的表现是"下次打开素材丢了、片段被移除"且**全程不报错**。三条都不能少：**引用集合要从所有项目算**（漏一个项目就删一批；副本共享 `sourceId` 是对的，所以"还有没有人用"只能全局回答）、**没有时间戳的旧记录当"这一轮先别删"并回填一个时间戳**（当成"没时间戳 = 很老 = 可以删"会把加时间戳之前导入的素材一次删光）、**导入了但一个片段都没放的素材照样算被引用**（按片段算会把它当孤儿，而素材库里那一行还在）。资产键的格式**只有一处定义**（`sourceAssetKey` 那三个函数）：读写两侧各写一遍 `source:${id}` 的话，清理侧算出来的引用集合会和存储侧差一个字，表现是"把所有素材都当孤儿"删光且不报错。存储读数**自己数不问 `estimate()`**（`usage` 会把 OPFS 导出残留混进来、与分项对不上账），**空态整条不出现**（编一个数字比沉默更坏，D25）。
- **离线素材的出路是"打开项目"这一刻，永不进 EDL**（`state/reidentify.ts` + `inspectProject()`，D37 第 2 刀）。打开拆两步：**先只验一眼**（只读，不装载不落盘）有哪些素材读不动，有就停在指认页；用户指完或明确跳过，才把时间轴装进 store。所以 `MediaSource` 不需要 `offline` 分支，合成 / 导出 / reader / 波形 / 代理**一行都不用改**。四条取舍：**种类不一致要拒绝**（拿 MP3 指认视频素材会让画面轨的片段引用一个没有 `fps`/`width` 的素材，代理去转一个没视频轨的文件、合成器拿到 0×0），而尺寸 / 帧率 / 时长不一致**只警告不阻止**（"我重导了一版 720p"很正常）；**指认结果一律按 `sourceId` 配对，不按顺序**（列表可排序，顺序不是身份，按下标配就是把 X 的文件配给 Y 且不报错）；**指认到的文件，元数据要跟着换、只保 `id`**（只换文件会留下陈旧的第二真值来源：`sourceGridFps()` 拿记着的 25fps 去换算一个 30fps 的文件就取到另一个时刻，`durationFrames` 偏长则尾部几帧解不出来、那一层画面静默消失）；**指认成功要落盘且文件与元数据成对写、先快照后文件**（写一半时"新元数据 + 老文件"仍落在离线这个安全桶里，反过来才危险），而**跳过一个字节都不写**——它是破坏性的，留给第一次编辑去写死，所以"打开看一眼再退回"出路仍然留着（实测：跳过后快照里片段还在、再打开还问；编辑一次才写死）。
- **自动存盘的生命周期跟着"当前项目装好了没有"走**（D37，改写自 D23 的三态——它守的陷阱没消失，只换了形态，而且多了一个更坏的）。判据是 store 的 `projectId`（null = 首页 / 装载中 / 刚删掉），两个都不报错的陷阱：一、**不能在项目装进 store 之前开工**——那时是空时间轴，早开一步就把要打开的项目覆盖成空的，表现是"卡片写着 12 个片段，进去是空的"；二、**切项目必须先停再起，收尾那次 flush 归旧项目**——autosave 的订阅回调和 flush 都从**同一次 `getState()`** 里取 `projectId` 和时间轴（两者由 `openProject()` 在同一次 set 里原子地换），捏一个启动时传进来的 id 的写法，防抖 1 秒内切完项目就是**项目 A 的内容盖掉项目 B**，不报错。推论：**删项目要先 `closeProject()` 再删**，否则 Editor 卸载收尾的 flush 会把刚删掉的项目原样复活。存 `File` 到 IndexedDB 是引用不是拷贝，所以**它会失效**，而失效时对象属性全都正常、只有真去读才抛——打开时必须真读一个字节验（同"别把某个浏览器给的元数据当成事实"）。
- **裁入点和切分的右半段要平移关键帧**（`shiftKeyframes`），在时间轴上平移片段则不要动它——偏移相对片段起点，只有"起点指向的内容变了"才需要跟着动。平移到片段外的关键帧**保留不删**：`valueAt` 区间外取端点值，语义仍对，入点拖回去动画能原样回来。
- **改关键帧的时间走 `moveKeyframe`，不是"删了重打"**（D32）。删了重打是两步撤销，而且中间那一瞬动画少一个点。缓动**跟着关键帧走**：它归左端关键帧所有、管的是它右边那一段（D10），所以它是这个关键帧自己的属性、不是位置的属性——留在原位等于把两段曲线都改了。**目标位置已有关键帧时拒绝，不覆盖**（覆盖会静默吃掉一个用户自己打的点）。偏移仍**允许落在片段之外**（同 `setKeyframe`，D10 的语义），夹回片段范围是界面的事。

## UI 约定（M1 起）

- **新增样式文件先作用域化**：`app.css` 限定 `.m0`、`editor.css` 限定 `.ed`。曾因 `app.css` 里的 `label` / `button` 裸选择器和同名 `.caps` 类，把编辑器状态栏和缩放滑块变成纵向排列。
- **像素与帧的换算只有一处**（`Timeline` 的 `pxPerFrame`），位置一律由帧号乘它算出。不要缓存像素值再反推帧号，缩放后必错。
- 改完 UI 要在浏览器里实测，不能只看类型通过——布局 bug（撑爆容器、播放头错位、文字重叠）只有渲染出来才看得见。
- **拖拽验不了就用真实指针输入，别用合成 `PointerEvent`。** `new PointerEvent("pointerdown", {pointerId: 7})` 派发出去时那个 id 没有对应的真实指针，于是 `setPointerCapture()` 抛 `NotFoundError`——而它在 `pointerdown` 处理器里、在挂 `pointermove` 监听**之前**，所以整条拖拽从不开始。表现是"拖了没反应、历史没变、落点没画"，**和"我的拖拽代码写错了"长得一模一样**。踩过一次（关键帧轨）。判据：先单独试一句 `el.setPointerCapture(7)` 看它抛不抛，抛了就说明是量法的问题；真实输入走 Playwright 的 `page.mouse.move/down/up`。
- **截图当证据时，状态要在同一次调用里摆好。** 同上一条那一轮：连着几次拖拽实验，每次松手都真的提交了一次移动，于是后面那张"非法落点"的截图其实拍到的是**合法落点**（红色的钻石根本不在里面），而我差点据此说"invalid 那条样式没生效"。凡是截图要证明某个状态，就在同一次调用里先把状态设成已知值再拍。
- **量"按一下会发生什么"时，按键和取样必须在同一次调用里**（**D49**）。验 J/K/L 时前两次读数一次是"跳到末尾"、一次是"一动不动"，两次都是量法坏了：追踪循环 1.5 秒就自己关了，而 `press_key` 那一趟 MCP 往返比它还慢，于是**量到的窗口整个错开在被测事件之前**。做法是在一次 `evaluate_script` 里派发**合成键盘事件**再原地取样（键盘处理器不看 `isTrusted`，这一点和 `PointerEvent` 那个坑不同——那边坏在 `pointerId` 没有真实指针）。真实按键单独验一次就够，用来证明 `e.key` / `e.code` 对得上。代价是**同一个 tick 里连按多下会被折叠**：处理器读的是 React state 里的倍率，同 tick 的第二下看到的还是旧值（真实按键之间有 React 的刷新，所以只是量法的限制）——要验"连按四下升到 8×"就得在每下之间留 80ms。
- **浏览器实测要用 `window.__kerfStore`**（dev 环境自动挂载），不要在脚本里 `import('/src/state/timeline-store.ts')`：Vite 的 HMR URL 带参数，动态 import 会拿到**另一个模块实例**，脚本改了状态界面毫无反应，看起来像 UI 没绑定 store。
- **模块级单例都要在 dev 挂全局**（`__kerfStore` / `__kerfResidency` / `__kerfWaveform`）。控制台里 `import('/src/xxx.ts')` 会因为 Vite 的 HMR URL 带参数而拿到**另一个模块实例**。这个坑踩过**三次**：store（改了状态界面没反应）、常驻量计量（量 30 分钟混音时峰值报 0）、波形缓存（第一次验波形时 `waveformSettled` 一直报 false，以为解码失败，其实读的是另一个空缓存）。**加一个模块级 Map/单例的同时就把它挂上去**，别等被它骗一次。
- **改完 store 的源码也要刷新页面**（**D50** 手验踩的），这是"模块级单例"那个坑的第三张脸：HMR 把 `timeline-store.ts` 换成新实例之后，**新模块在导入时把 `window.__kerfStore` 重新指向了自己**，而 React 还绑在旧的那个上。读数是 `projectId` 为 null **而编辑器界面还开着**（看起来像"项目状态丢了"），改动本身完全无辜。判据同那条"红了先单独重跑一遍"：**动过 store / 单例的源码之后，先刷新再量**。
- **改完自检的源码要刷新页面再跑**，同一个坑的另一面：`import('/src/dev/verify-xxx.ts')` 会拿到**已缓存的旧模块**，于是你验的是改之前的代码。踩过一次——断言结果和实际像素对不上，查了半天才发现跑的是旧版。
- **颜色字符串的解析与序列化只有 `compose/css-color.ts` 一处**（D33），认的范围就是我们自己写得出来的范围（`#rgb` / `#rgba` / `#rrggbb` / `#rrggbbaa` / `rgb()` / `rgba()`），具名颜色和 `hsl()` 一律不认。序列化**定死一种形式**：不透明给 `#rrggbb`、半透明给 `rgba(r, g, b, a)`，**不给 `#rrggbbaa`**——八位十六进制万一某个环境不认，失败形态正是下面那条"赋值被静默忽略"。定死一种形式还让同一个颜色永远给出同一个字符串（栅格缓存的键里带着整份样式，两种写法会变成两条缓存）。
- **写进 EDL 的颜色要在编辑入口校验**（`setTextStyle` 里那三个颜色项）。`ctx.shadowColor = "乱码"` **不抛错**，整个赋值被忽略、保持上一个值——新建的上下文里那是透明黑，表现是"颜色调了没反应"。
- **`input[type=color]` 吐不出 alpha**，所以带透明度的颜色一律拆成"原生取色器 + 一个不透明度百分比"（`Inspector.tsx` 的 `RgbaRow`）。动色板时**必须保住 alpha**（只换 RGB），那正是这个控件存在的理由；自绘色轮属于 PLAN.md §9 里推后的东西。行内顺序跟「描边」那一行一致：数字在前占满、小色块收在末尾——色块用 `.color`（宽度 100%）会把输入框挤成一条缝，踩过一次。
- **界面上"看得见的降级"不要依赖异步状态**（D36）。图片解码尺寸那一行第一版读 `decodedImage()` 的缓存，于是渲染那一刻还没解好就什么都不显示，而解好之后**没有任何东西触发重渲**——那条降级实际上看不见（实测 6000×4000 的图只显示了原图尺寸）。要说的本来就是"我们会把它解成多大"，用纯函数 `decodeSizeFor()` 直接算，不需要等。凡是"提示某个取舍"的界面，先问它依赖的那个值是不是异步来的。
- **检查器里的单位换算只有 `Inspector.tsx` 这一层**：EDL 存弧度 / 倍数 / 占输出高度的比例，界面显示度数和百分比，换算收在 `PROPERTY_SPECS` 的 `toDisplay` / `fromDisplay` 一对函数里。散到各个输入框上就会出现"这个框按度算、那个框按弧度算"，而画面上只表现为"转多了"。
- **属性有关键帧时，输入框改的是播放头那一帧的关键帧值**，没有时改的是静态变换（D10 的"并存"在界面上的必然形态）。所以播放头不在片段内时动画值不可编辑——那时"当前帧"没有意义，硬写会打出偏移为负的关键帧。
- **"保音高"做成下拉不是勾选框，而且原速下整行不出现**（**D40**）。勾选框的未勾状态会读成"默认/中性"，而它的实际含义是**声音会变调**——那个结果得被指名，不能让用户从"我没勾保持音高"里自己推出来；两边都有代价（重采样变调 / 伸缩慢且抹瞬态），所以两句提示都写出来（同 D21 那两条淡化曲线不是风格选项）。**原速下这一行整个不出现**：`clipPreservesPitch()` 先判速度，所以那时选择不产生任何效果，摆一个不起作用的控件同样是在说假话；但**字段不跟着清掉**，调回 2× 时选择要还在（`setClipPreservePitch` 允许原速下开关正是为此）。无音轨素材上同样不出现，同 D39 那条。这一行要放在**和「倍数」同一个 `fields` 块**里——另起一块会多出一档间距，读起来像两组无关的设置。
- **速度那一节的数字输入复用 `NumberField`，不要手搓**（D39）。它已经实现了下面那条"外部值变了要丢草稿"的纪律，而手搓一个的表现是**点了预设之后输入框还显示上次输入的数字**。预设排成 **3 列两行的 grid 而不是 flex-wrap**——6 个按钮在 210px 宽的面板里排不成一行，自动换行会得到 5+1 的参差版式（实测过）。**变调提示只在"速度不是 1× **且** 这个素材真有音轨"时出现**：给一个无声的画面片段挂"声音会变调"是句假话，而产品里的假话要删不是补充说明（同 D37）；反过来变调本身**必须说出来**，静默变调就是硬规则 10（同定格帧数报到检查器）。**波形和缩略图铺开的是源片区间，要乘速度**（`clipSourceFrames` / `speedFactor`，并且**两个都要进 effect 的依赖数组**，否则改了速度不重画）——这一行本来就有两个不同的 fps，加上速度是第三个量，漏乘的表现是"波形只画出前一半然后停住"而两者各自看都像对的。
- **带草稿态的受控数字输入，必须在外部值变化时丢掉草稿。** 只在 blur 时清草稿的话，拖播放头时输入框会一直显示上次输入的数字——动画值明明在变、框里纹丝不动，看起来像关键帧没生效。判据是"当前值还等不等于自己刚提交出去的那个"，踩过一次。
- **时间轴上的关键帧轨只给"这条轨上被选中的那个片段"展开**（`Timeline.tsx` 的 `KeyframeLanes`，D32），一个属性一行，轨道头上的菱形开关**只在有动画时才出现**。全都展开的话一条有动画的字幕轨能顶出十几行；而"整轨所有片段的动画叠在一行"根本读不出来——两个片段可以在同一个偏移上各有一个点。
- **拖关键帧走"拖动中只画落点、松手才提交"**（同 `use-clip-drag`），于是一次拖拽只进一条历史，**不需要**带关键帧身份的合并键。D10 的重新评估条款担心的是"边拖边提交"那种写法，那时才需要那种键——而关键帧的身份只有偏移，拖动中偏移一直在变，那个键根本立不住。低于阈值算**点击**（跳到那一帧），没有阈值的话想点一下的手抖一像素就变成一次进撤销栈的编辑。
- **钻石要用 `margin-left: -4.5px` 把中心对到帧的左边界上**——播放头也画在那里（`left: playhead × --pxf`），不对齐的话"拖到播放头"看起来永远差半格。实测两者 `delta = 0`。
- **多选的三条手势纪律**（**D42**）。**⌘/Ctrl 按下 = 加减选择，不启动拖拽**（加选那一下顺手把片段拖走是"选了 A 拿到 B"，用户的手势是"再选一个"、位移只是手抖）。**已经在多选里的片段，按下时不要把选中收缩成它自己**——那样一按就丢掉整组，而用户接下来正是要拖这一组；代价是**松手时要按拖拽阈值补上"点击"的语义**（低于阈值就收缩成这一个），不补的话从"选了三个"回到"只要这一个"就只剩 Esc 再点一次这条路，而用户明明点了一个片段却看到三个还亮着。**按住边缘裁切永远只作用于一个片段**，所以那时直接把选中收缩掉（多选裁切要么按比例缩、要么全裁同一个量，两种都不是明显正确的）。
- **整组拖拽的幽灵是一组，而合法性对整组是一个布尔**（D42）。分别判会画出"这几个绿那几个红"，而提交是全体或拒绝——那就是界面说了一件不会发生的事。`Ghost` 因此带上 `clipId`（React 的 key，也让"这个落点是谁的"在读数里看得见）。**多选拖拽不换轨**，幽灵各自留在自己那条轨上，所以这条降级看得见（同 D19 定格帧数那条"标注而不禁止"）。
- **批量改属性的失败语义随 D42（各自独立），而"共同读数"不能折叠成其中一个片段的值**（**D45**）。三件事收在三个函数里：**`propertyApplies(clip, track, property)`** 是"这个属性对这个片段有没有意义"的**唯一**判据（单选那两条 `track.kind` 的 JSX 门也问它——两处各写一遍，漂了的表现是"单选看得见这一行、批量里没有"，或者更坏：批量把音量写进视频轨的片段，字段存下来了而声音一点没变，因为 `planAudioJobs` 只混音频轨，而 **`setClipVolume` 自己不判轨道种类**）；**`summarizeProperty()`** 给 `editable` / `animated` / `value` 三个数，**值不一致时 `value` 是 `undefined`**、界面显示「多个值」；**`setClipsProperty()` / `resetClipsProperties()`** 是写入口。**读和写必须用同一份名单**：有动画的片段两边都排除——批量写的是静态值而那时画面上生效的是关键帧，算进"共同值"就是显示一个改不出来的数字。**重置刻意不跳过有动画的片段**（与改值相反），它的语义就是"把静态值恢复成默认、不动关键帧"。**挑其中一个片段的值显示是这一刀里最坏的选项**：它连当前状态都说错了，而改一下之后 N 个真的都变成那个值——**这个谎自己变成了真的**。
- **`perClip()` 那条"逐个做"的循环必须分三类，不是两类**（**D45**）。`changed:false` 且**不给 `reason`** 的那个结果是"值没变"（`EditResult` 那条约定），它既不是成功也不是失败：算进 `skipped` 的话，"给 5 个片段设 50% 而其中 2 个本来就是 50%"会报成"5 个里有 2 个没改"——那是句假话，而且只在"部分片段的值本来就对"时出现，最容易漏测。批量删除和批量改属性共用这条循环（它们的失败语义同属"各自独立"那一类），措辞靠一个 `verb` 参数留在各自手里。
- **写入侧的"静态值存在哪儿"也只有一处**（`setClipProperties`，D45），它是 `staticValueOf` 的镜像。两边各写一遍 `isAudioProperty ? … : isColorProperty ? … : …` 就会在加第四组时漏掉一处，而漏掉的表现是"这个属性批量改不动"或者"改了但读回来还是老值"。都不报错。
- **带草稿态的数字输入遇上"折叠过的读数"时，判据要放宽**（**D45**）。`NumberField` 原来的判据是"外面的值还等不等于我提交出去的那个"，而多选时外面那个值是 N 个片段折成的一个数：**"没有共同值"（`null`）根本不是"我刚提交的值没生效"的证据**——有片段在锁定轨道上时它**永远**不会收敛。判据搞混的表现是**多位数打不进去**：敲"7"提交 7%，几个片段仍然不一致于是草稿被判作废、框清空，接着敲的"0"就成了一个全新的 0，用户想输 70 拿到 0，**而且不报错**（实测 `v1: [0, 0]`）。所以两个 `null`（还没提交过 / 没有共同值）都表示"没有可比的东西"，都要留住草稿。**这是同一族的第三次**（D44 的夹紧振荡、D42 的框选布局快照）：判据的输入被判据自己的结果污染。配套的量法一条：**`input[type=number]` 对非法中间态（`"-"`）本身就返回 `""`**，拿它验"草稿留住了没有"量不到东西——单选下同样是空，那是浏览器行为。
- **多选面板里"这一节作用到谁"要写出来**（D45）。标题带个数（「变换 · 2 个」「音量 · 3 个」），因为多选很容易横跨两种轨道；**一组属性对选中的片段都没意义时整节不出现**（同 D40 那条"摆一个不起作用的控件同样是在说假话"）。**有动画的按属性点名成一行**（"亮度 2 个有动画，批量改不到"）而不是每行一个角标：`.f.ctl` 第三列只有 20px（关键帧钻石的宽度），塞进去会把输入框挤成看不清数字的一条，那条注释记着已经踩过两次。**批量里没有关键帧按钮**——批量打点需要一个共同的"这一帧"，而播放头只落在其中一部分片段里。
- **轨道的三个开关必须进撤销栈，而 `locked` 是唯一不判"轨道已锁定"的操作**（**D43**）。三个字段（`locked` / `muted` / `hidden`）都在 `Timeline` 里，而历史存的是**整份 Timeline**：绕过 `apply()` 直接改的话，之后任何一次撤销都会把这个开关连带回滚。而它本来就该是编辑——静音和隐藏**会改变成片**（`mix-plan` 跳过静音音轨、`videoTracksInDrawOrder` 跳过隐藏画面轨）。`muted` 只能给音频轨、`hidden` 只能给画面轨，装错了要拒绝（给画面轨设 `muted` 不报错也没效果，按钮亮着而声音照旧，是 D19 那类"存了但不生效"）。**状态要在轨道上看得见**：淡化**片段**（不是整条 lane——网格线和播放头要保持原样，否则整条轨看起来像被禁用了）、锁定给 lane 铺斜纹。开关的**标题写"按下去会发生什么"**而不是这个开关叫什么：写死「静音」的话，已经静音的轨道上它读起来像"这里是静音的"。
- **右键菜单的关闭听 `pointerdown`，而夹进视口的算式必须是不动点**（**D44**）。右键顺序是 `pointerdown → contextmenu → pointerup`，菜单在 `contextmenu` 上开——监听 `pointerdown` 天然不会自己关掉自己，监听 `pointerup` 会被同一次右键的抬起立刻关掉（表现是"菜单闪一下就没了"）。夹紧那一步第一版是**振荡**的：读已经挪过的矩形、比原始 `x/y`，于是在两个值之间无限跳，React 报 `Maximum update depth exceeded` 组件直接崩、第一次右键就没有菜单——**typecheck 和单测都碰不到，只有浏览器里点一下才知道**。改完之后右边缘还差 6px，因为 `position: fixed` 不给宽度时按可用空间收缩，贴边打开时量到的是被挤到 `min-width` 的宽度（实测 168 vs 自然宽度 174），要加 `width: max-content` 让**量出来的尺寸与它现在在哪无关**。菜单项**只灰掉"确定做不到"的并把原因写在行内**（hover 才看得见的解释等于没解释，同 D3），删除刻意不灰——多选横跨锁定与未锁定轨道时它是"部分做得到"，灰掉等于把能做的那部分也挡了。
- **定格状态必须在片段上看得见**（`.frz` 那个角标，**D48**）。缩略图条铺满同一张图已经是暗示，但**"定格了"和"这段素材本来就静止"是两个结论**，只有软件知道；不标的话用户看到的是"拖了播放头画面不动"，那和素材坏了长得一模一样（同 D43 那条"静音 / 隐藏要在轨道上看得见"）。**定格入口不做批量**：它要一个共同的"这一帧"，而播放头只落在选中的其中一部分片段里（同 D45 那条"批量里没有关键帧按钮"）；多选时那一项**整个不出现**而不是灰掉——灰一个永远点不到的东西只会让菜单变长，而"播放头不在片段内"那种灰掉是有用的（移一下就能点）。
- **验菜单关闭时不要把 `pointerdown` 派发到 `window` 上**（**D48** 手验踩的）。`ContextMenu` 的判据是 `ref.current.contains(e.target)`，而 **`contains(window)` 抛 `TypeError`**（参数不是 Node），处理器在调 `onClose()` 之前就断了——读数于是是"菜单关不掉"，而真实点击永远 target 一个元素。派发到 `document.body` 上立刻就关（实测菜单数 1 → 0）。同那条"合成 `PointerEvent` 的 `pointerId` 没有真实指针，`setPointerCapture` 会抛"——都是**量法自己坏了而读数长得像产品坏了**。
- **量缩略图条要先确认这条足够宽**（**D48** 手验踩的）。一个 tile 的宽度是"缩略图按行高等比"（16:9 在 32px 行高上约 57px），而默认缩放下 150 帧的片段只有 63px——**5 个采样列全落在同一个 tile 里，于是不定格时也读到"每一列颜色都相同"**，和定格的正确读数一模一样。放大到 400 之后（1200px、约 21 个 tile）量法才看得见帧差，最终读数是不定格 5 列 5 色、定格 5 列 1 色。凡是"这一条铺了很多份"的断言，先算一遍采样点会不会落进同一份。
- **J/K/L 的对向键是"退一档"，K 才是停，而变速读数只在"不是常速正放"时出现**（**D49**）。4× 正放按 J 得到 2×，一路退过 1× 才到 -1×——这是 Premiere / FCP / Avid / Resolve 四家一致的行为，也是**唯一能减速的办法**（若 J 直接跳到 -1×，从 8× 慢下来就无路可走）；配套地**暂停不在梯子上**，否则"1× 按 J"会落到暂停而不是倒放，而那一下用户要的是往回看。**空格在变速中是"停"不是"回到常速"**（它一直就是播放/暂停那一个键）。读数 chip 只在 `isShuttling` 时出现——常速正放摆一个恒显示「1×」的读数是噪声，而**变速不出声这件事只在这条 chip 上说得出来**（有音轨才提，给一条没有音轨的时间轴解释"为什么没声音"是句假话，同 D39 那条"给无声画面挂『声音会变调』"）。**预览的键盘处理器要拒掉带修饰键的按键**：⌘K 是切分（`Editor.tsx` 也挂在 window 上），被这里的裸 K 吃掉的表现是"切分快捷键有时候不灵"，而两个处理器谁先谁后不可靠。**两端边界都要判**：只判正向的话倒放到 0 帧会一直空转（`setPlayhead` 夹在 0、rAF 永不停、声音也不会被停掉）；**正放到末尾按播放 = 从头再放一遍**（原有行为），而**倒放到 0 再按 J 什么都不做**——把用户扔到末尾去是"选了 A 拿到 B"。
- **轨道菜单和片段菜单是两个状态，共用 `ContextMenu`**（**D47**）。菜单项来源完全不同（一个看轨道、一个看选中集合），而 window 级的 `pointerdown` 会先把旧的关掉，所以同屏不会出现两个。**轨道菜单挂在轨道头 `.th` 上，不挂整条 `.lane`**——lane 里的右键属于片段菜单，两个抢同一块区域会让"右键空白处"的含义不稳定。工具栏那个「+」开的是**同一个菜单但没有目标轨**（`trackId: null`，于是只有两条新建项），落点按**按钮的矩形**算而不是指针处：那是点按不是右键，菜单该跟着控件走。
- **素材行里要放按钮，那一行就不能是 `<button>`**（**D47**）。`button` 套 `button` 是非法标记，浏览器会把内层拆出去，表现是删除按钮跑到行外面或者点不动。改成 `div` 之后**顺带踩到一条**：`.asset > span:last-child` 那句 `min-width: 0` 原来指的是文字列，末尾多了按钮之后它指向按钮，于是长文件名不再截断、把整行撑出面板（要改成 `nth-child(2)`）。凡是往一行的末尾加东西，先查一遍有没有 `last-child` 选择器指着原来的最后一个。删除按钮**用透明度显隐而不是 `display: none`**：后者会在 hover 的那一瞬改变列宽，整行跳一下；而透明的可聚焦按钮是个陷阱，所以 `:focus-visible` 也要让它显形。
- **I / O 打入出点、⌥I / ⌥O 清，而判据是 `e.code` 不是 `e.key`**（**D50**）。macOS 上 ⌥I 是死键（`key` 变成 `Dead` 或 `î`），按 `key` 写的话"清除入点"**只在 mac 上不响应**——那种只缺一个平台的快捷键最难被发现。**标记画在标尺上，不贯穿所有轨道**：它是导出范围不是"这一段被选中了"，铺满整个时间轴会读成后者（同 D43 那条"淡化片段而不是整条 lane"）；`pointer-events: none` 是必须的，挡住的话在标记区间内点一下不动播放头，看起来像标尺坏了。**两个爪画的是字段本身，不是 `markedRange()` 的两端**——只打了入点时区间的右端是时间轴末尾，在那儿画一个出点爪就是说了一件用户没做的事。**读数 chip 的颜色要和标尺上那段区间同族**（这一刀第一版沿用了 D49 变速那个琥珀色，而标记是蓝的——读数和它描述的东西不同色时，用户得先把两者对上才知道这行字在说什么）。**导出面板的「范围」是三选一不是两个布尔**（整条 / 标记 / 选中），而**有标记时默认选标记那一档**：打标记这个动作的全部意义就是"我要导这一段"，默认还停在"整条"等于让用户每次再选一次，而漏选的后果是几分钟之后才发现导出了一条长得多的片子。
- **框选的判据是纯函数 `clipsInBox`，而交互里有一个真实的反馈环**（**D42** 第 2 刀）。判据两条：**碰到就算、不要求完全框住**（否则比可视区还长的片段永远选不中）、**边界和 `overlaps` 同一套左闭右开**（两处不一致会让"框到贴边"时选中数忽多忽少；推论是零宽的框选中它穿过的片段，而框落在交界上时两边都不选中——第一次实测的取样点正好撞在这上面）。交互三条：**垂直命中用像素矩形、不数轨道序号**（关键帧轨会插在轨道之间，它的 lane 刻意没有 `data-track-id`）、**那些矩形必须在按下那一刻取快照**、**低于阈值 = 点空白处清空选中，而 ⌘ 点空白处什么都不做**（⌘ 是"加选"，加上零个应该保持原样）。快照那条是这一刀唯一的真陷阱：关键帧轨只在"恰好选中一个"时展开，框选一旦选中第二个它就收起、底下轨道**在拖动中往上跳一行**，每次重新量的话框覆盖的轨道就跟着跳（实测同一个框：快照版 **3 段**、重新量的版本 **6 段**——A1 整条被吞进来）。凡是"拖动中按布局做判据"的地方，先问这次拖动会不会自己改变布局。
- 拖拽类交互要**同时测水平和垂直**：跨轨道是纯垂直移动，只测水平会漏掉阈值判定的 bug。已经踩过一次的具体形态是**"没动就不提交"的判断只看帧号**——跨轨时帧号一个都不变，于是整个落点被静默丢掉（不移动、也不给 `lastRejection`，表现成"松手后弹回原轨"）。凡是判"这次操作等于没动"的地方，都要把轨道一起算进去。

## 导出层约定（M1.5 起）

- **预览和导出的取帧决策只能来自 `src/edl/sampling.ts`**。共用 `compose()` 只保证"画法一致"；"该画哪个片段、按什么顺序、读源片哪一刻、这一层的变换是什么"如果各写一套，画面照样不一致。三个函数是唯一入口：`sourceMicrosAt`（帧 → 源片时刻）、`videoTracksInDrawOrder`（z 序反转只有一处）、`visibleVideoClips`（某帧的图层 + 已求值的变换）。
- **导出的逐帧循环是"两步"，不能合成一步**：先把**每条**轨的 reader 都推进到这一帧（包括这一帧没有片段的——空档轨要被问到才会释放解码器，而且 reader 只允许向前问，漏问一帧再也补不回来），存进 `Map<trackId, sample>`；再按 `visibleVideoClips` 给的顺序装配图层。只问"有可见图层的那几条轨"会同时踩上面两条。
- **取帧三函数只接 `MediaClip`**（`sourceMicrosAt` / `sourceCenterMicrosAt` / `toSourceFrame`），文字片段编译期就传不进去。`visibleVideoClips` 返回的是**素材层和文字层混排的联合**（`VisibleClip`），不拆成两个函数——谁压谁是同一个 z 序问题，拆开等于让调用方再合并一次顺序。
- **不要用 `toSourceFrame()` 做取帧位置**。它算的是帧号加减，隐含"源片帧率 = 时间轴帧率"。25fps 素材放到 30fps 时间轴上会慢 20% 且不报错。用 `sourceMicrosAt`。
- **`VideoTrackReader` 返回的 `VideoSample` 归 reader 所有，调用方不要 `close()`**。时间轴帧率高于源片帧率时同一个 sample 会被多个输出帧复用。
- **取帧只能向前**。倒着问会抛错——顺序解码是硬规则 3 的前提。
- **每条轨一份 `Input`**，哪怕同一个源文件：Input 的 demuxer 有读取位置，两条轨交错拉包会互相打乱。**转场窗口里同一条轨要同时开两个游标**，那两个游标即使读同一个源文件也必须各自一份 `Input`——同一条约束在轨内的翻版。所以 Input 按源片**分池借还**（`VideoTrackReader.borrowInput`），池深被"一帧最多两层"限住，不随片段数增长。
- **纯音频素材要在借 Input 之前就退出**（`openCursor` 里的 `source.kind !== "av"`）。它没有视频轨，靠后面那道 `!videoTrack` 检查也能兜住，但那样每一帧都会借一次 Input 再还一次，白开一遍 demuxer。判据是 M0 那条"配乐：导出没有为它开解码游标（泄漏为 0）"。
- **reader 返回值按 `clipId` 索引，不是按轨**，解码区间用 `clipRenderSpan()` 而不是片段占位——转场窗口跨过交界，出场片段要多解出点之后一段、入场片段要多解入点之前一段。按占位开区间的话那些帧解不出来，**成片里转场的一侧是黑的而预览是对的**。
- **音频混流只能在主线程**（`OfflineAudioContext` 在 Worker 里不可用），混好的 PCM 要 **transfer** 进 Worker，不能靠结构化克隆——几百 MB 会整份复制一遍。
- **混音按段产出，Worker 拉、主线程供**（M3，`audio/mix-segments.ts` + 协议里的 `audio-pull` / `audio-chunk`）。整条混完的峰值随片长线性涨（30 分钟实测 989MB，一小时约 2GB），分段之后**峰值与片长无关**——自检实测 4 倍片长比值 **1.00**。**必须是"拉"不能是"推"**：推的话主线程会尽快把所有段发过来，Worker 那边照样堆成整条 PCM，一个字节都没省，只是从主线程搬到了 Worker。
- **段边界必须落在整数输出样本上**（`sampleAlignFrames`）。一帧对应的输出样本数通常不是整数（29.97 下是 1601.6），而同一个片段在相邻两段里的 `start(when)` 会被 Web Audio 落到最近的采样点——两段的 `when` 相差非整数样本时各自取整，接缝错开一个样本 = 一声轻微咔哒，而 RMS 包络断言对它**完全免疫**。对齐量取 `num / gcd(num, sampleRate × den)`，29.97 / 59.94 下是 5，25 / 30 / 23.976 下是 1。
- **音频的起播时刻不经过微秒**（`mix-plan.ts` 的 `exactSeconds`）。别处的秒一律走 `microsToSeconds(frameToMicros(...))`，但微秒取整在 48kHz 上是 **0.048 个样本**，而 `start(when)` 是亚采样精确的。实测：分段与不分段差 **5.22e-4**，恰好等于 0.016 个样本相位差在 0.25 幅度 1kHz 正弦上的值；改成精确有理数后降到 1.8e-12。这不违反硬规则 1——帧运算仍是整数帧号，只是换算成秒的那一步不再多绕一道量化。
- **每个片段一个只向前推进的解码游标**（`ClipAudioCursor`），一次 seek、之后只向前读，**不要每段各自 seek**。踩过：每段重新 `sink.samples(start,end)` 在 Chrome 上没事，**Safari 上从第 9 段起波形相位分毫不差、幅度却差 5.7% 并逐段放大到 26%**——重新 seek 之后解码器吐出来的头几百毫秒不完全正确。把 pad 从 0.167 秒加到 0.5 秒确实能盖住，但那是把正确性押在一个凑出来的常数上；一次 seek 之后分段与不分段就是**结构上**相同。同"取帧只能向前"，也同上一轮查 Safari 那 9.3ms 的教训。
- **相邻两段的源片区间是重叠的（2 × pad），游标要留回看队列**。不留的表现是**除第一段外全部静音**，波形打出来是一排 0.000。队列只保留重叠那么长，不随片长增长。
- **段边界不能变成编码器的输入边界**（`makeAudioPump` 的 staging）。段长几乎不可能是喂块的整数倍（段 24024 样本、喂块 24000），直接从段里切就会在每段末尾多喂一个 **24 样本的碎块**；Chrome 没事，**Safari 上成片淡出曲线整体偏高约 0.05**（内容晚了约 33ms）。攒够一整块再喂，只有整条最后一块可以短。
- **每段一个 `OfflineAudioContext`，渲染完必须显式 `close()`**。Safari 对同时活着的 AudioContext 有硬上限，靠 GC 回收赶不上建的速度：切 12 段时**第 9 段起渲染结果直接变成静音**（不抛错、不警告），段再多就把页面卡死。`close()` 在规范里没明确给 `OfflineAudioContext`，所以特性探测着调。
- **导出预设按"每像素每帧比特数"定档，不写死码率**。写死过一次：360p 素材上「标准发布」仍按 10 Mbps 编，白扔 5 倍字节，而「存档母版」倒挂成最低档。
- **预设按短边封顶，不按高度**（`maxShortSide`）。竖屏的「1080p」是 1080×1920。按高度封顶过一次：1080×1920 被压成 608×1080，像素量只剩三分之一而标签仍写「1080p」，成品能播、比例也对，自检抓不到。见 PLAN.md 的 **D7**。
- **内存要自己数，不要问浏览器**（`export/residency.ts`）。`measureUserAgentSpecificMemory()` 需要 COOP/COEP，那套头会打断页面里的第三方脚本（和不上 ffmpeg.wasm 多线程版是同一个理由）；`performance.memory` **只报 JS 堆**，而导出的内存几乎全不在 JS 堆上——解码帧、画布后备存储、解码器缓冲都不算进去，一条长片能把显存吃光而那个数几乎不动。做法是在借出/归还处各记一笔，记账点要**贴着 `close()` 写**，在外层"大概算每条轨两个"算出来的是设计意图不是实际持有量。
- **记账点要贴着"最后一个引用消失"的地方写，不是"逻辑上用完了"的地方。** 踩过：混音里解码出来的 PCM 在 `startRendering()` 之后就销了账，但它们还挂在 `AudioBufferSourceNode.buffer` 上、被 `ctx` 引用到函数返回——峰值少报了整整一份（1318 vs 1648 MB）。计量报的是"我们还引用着多少"，引用没断就不能销账。
- **只报峰值不报位置等于没报。** "峰值 800MB 且早就稳住"和"峰值 800MB 且还在涨"是两个结论，前者与片长无关、后者是长片必崩。所以 `ResidencyReport` 同时留峰值帧号和首尾两个采样点。
- **峰值要采在峰值发生的那一刻，不是"这一步做完之后"。** 混音的峰值在 `renderSegment` **内部**（解码结果和渲染目标同时活着），所以混音器给了一个 `onSample` 钩子；在 `next()` 返回之后采，两者都已经销账，读到的是谷值。这条是反向验证逼出来的：把采样点挪到段间，读数从 2.0MB 掉到 751KB（上一段交出去的 PCM 还挂着），而"峰值 > 0"那条护栏**照样全绿**。现在的判据是"峰值 > 1.5 × 单段输出"——错误采样点恰好是 1.0×。
- **交叉淡化的排期是纯函数（`audio/mix-plan.ts`），接线才在 `mixdown.ts`。** 会算错的是排期——解码区间要按 `clipRenderSpan()` 往两边撑开（按占位开就是"淡化只淡了一半"）、窗口被导出区间切一半时曲线要从对应进度接上、一个片段同时是入场和出场时两条包络怎么排。而 `OfflineAudioContext` 在 node 里造不出来，混在一起就只能靠端到端自检去撞，撞到了还分不清是排期错了还是接线错了。同状态层那条"判断写进纯函数"。
- **音频余量不足时静音，不定格。** 画面那边定格边缘帧（D19），声音**不能定格**：按住最后一个采样点是直流台阶，松开时"啪"一声。所以源片区间照常按窗口算、**允许为负也允许超过源片末尾**，游标把解不到的部分留成零。相应地向解码器要的是**夹紧过**的区间，而写回偏移仍按**未夹紧**的起点算——两处用同一个值会把前半段静音吃掉，表现是入场侧的淡入整体提前。
- **恒等增益不穿 `GainNode`**，同 `isDefaultGeometry` / `isDefaultColor`。不是性能优化：没有转场、也没调过音量的项目必须和加这些功能之前**逐样本一致**，否则 M0 第 17 项那条音画同步断言会莫名其妙开始漂，而漂的原因和它们毫无关系。判据现在有三项（`ramps` 为空、`baseGain` 为 1、`volume` 为 1），漏掉任何一项的表现都是"调了没反应"而不是报错。
- **片段音量和转场淡化是同一条增益链上的两个来源，合成方式是相乘、只穿一个节点**（D27）。静态音量是常数，乘进起始值和每条曲线的采样点就是精确合成；串两级 `GainNode` 也对，但那样"没调音量"的项目要靠"第二级也走快路径"才维持得住逐样本一致，判据从一个变成两个。**排期层刻意不把音量乘进 `baseGain`**（`AudioJob.volume` 单独留着）——那一层唯一的用处是可单测，乘在一起之后"淡化进度算错了"和"音量传错了"在返回值上就分不开。相乘在 `mixdown.ts` 的 `envelopeInput`。
- **音量打了关键帧时，排期层把淡化 × 包络逐帧乘成一条 `gainCurve`**（D28），`envelopeInput` 只喂它、**不再喂 `ramps`**：`AudioParam` 上同一段时间喂两条 `setValueCurveAtTime` 会直接抛错，而那条曲线里已经含着淡化了，再喂一遍等于乘两次。预乘而不是串第二个 `GainNode`，一是让乘积的算术留在纯函数里（会写错的正是"淡化窗口内外怎么接""包络在片段坐标系而淡化在导出坐标系"，而写错只表现为"声音大小不对"），二是没打关键帧的项目一个字节都不变。
- **`crossfadeGainAtFrame` 的语义必须与喂给 `AudioParam` 的那串 `setValue*` 逐点一致**：起点 `baseGain`、进到包络里取当前进度、**越过之后保持末值**（`AudioParam` 曲线结束后就是这样，不会跳回 1）。护栏是"恒定包络的成片与静态音量逐样本相同"——它同时钉住两条路径不分叉。
- **包络的帧偏移相对片段起点，淡化和采样点在导出坐标系**，所以逐帧求值时要减 `clip.timelineIn`。混用的表现是"包络整体偏移了一个片段起点的量"，而**片段恰好从 0 开始时完全正常**，拖到时间轴中间才出错。
- **音量为 1 时一个采样点都不碰**（不走那遍乘法循环）。`× 1` 在浮点上确实是恒等，所以这不是精度问题；它保证的是"没调音量的项目"连代码路径都和以前相同。反过来也是这条的代价：**不能拿"volume:1 与不设该字段逐位相同"当护栏**——快路径坏掉时那条读数和健康值一模一样，测的是运气。
- **音量为 0 的片段照常排期，不顺手跳过**。"静音就别解码了"只对**静态**音量成立；做包络之后 `volume` 随时间变，"这一刻是 0"完全不意味着整段是静音，那时这个优化会静默吃掉声音。
- **两条淡化曲线不是风格选项，是可测的 3dB。** 等功率（平方和恒 1）对不相关素材对，等增益（和恒 1）对相关素材对。实测同一段 1kHz 正弦的两个位置做淡化：等功率在交界处两层之和鼓到 **1.404 ≈ √2**，等增益平在 **0.989–0.995**。所以不能替用户挑。
- **音频进度是连续的，不取帧中点。** 画面那边 `transitionProgress()` 取帧中点（帧是区间，一帧只能有一个颜色），声音不是。硬套帧中点会让整条淡化偏移半帧，两侧各偏就是接缝处一个 33ms 的功率缺口。
- **混音的峰值比最终 PCM 大得多，而且它在主线程。** 30 分钟 1080p 实测：导出循环峰值 665MB 且完全平，**混音峰值 1648MB**——三份 PCM 同时活着（各素材解码结果 + `OfflineAudioContext` 渲染目标 + 拷给 Worker 的那份）。所以量长片内存**必须把 mix 阶段一起量**，只量 Worker 会得到"还有大把余量"的反结论。计量器是每个 JS 上下文一份，主线程那份由 `client.ts` 喂。
- **混音结果直接 transfer 给 Worker，不要先拷一份**（`takeChannels`）——上面三份里的第三份已经这样砍掉了（1648 → 989MB）。前提是每个声道整块独占一个 ArrayBuffer 且各声道不共用，这是**规范灰区**（Web Audio 只规定 detach 后 `getChannelData()` 返回零长数组），所以代码每次当场验（`channelsAreMovable`），不满足就退回拷贝、并让多出的那份出现在计量里。**别再把"直接 transfer 会 detach，rendered 就坏了"当成拷贝的理由**——`rendered` 在那之后就没人要了。相应地 `MixedAudio.channels` 是"交出去就没了"的数据，post 之后不能再读。
- **导出跑完 `leaked*` 必须全是 0。** 解码帧和解码器都该在 reader 的 dispose 里还回去，非 0 就是泄漏——而泄漏在几百帧的自检片上完全看不出来，到 30 分钟才会变成标签页崩掉。
- **音频编码器有 priming，容器补偿不了，只能在喂 PCM 时丢掉头部**（`audio/encoder-delay.ts`）。WebCodecs 的 AAC 编码器输出比输入晚 **2112 样本 = 44ms**，并把 priming 标成从 t=0 开始，`decoderConfig` 里不给延迟值；mediabunny 的 `edts` 只会"把整条轨往后推"，写不出"跳过前 N 样本"。所以成片前 44ms 必然是 priming，补偿办法是 `fed[j] = pcm[j+D]`。**延迟每次导出实测，不要写死 2112**——它来自编码器实现，写死赌输的表现是音画偏移且不报错。Opus 路径实测偏移为 0（`pre_skip` 是标准字段，mediabunny 写了）。理由和放弃过的三个方案见 PLAN.md 的 **D14**。
- **`StreamTarget` 的 `chunked` 必须是 false**，它不是性能开关。开着（攒到 16MiB 再落盘）在 Safari 上会让成片大小**恒为 16MiB 的整数倍**：0.5MB 的片子报 16.8MB，32.1MB 的报 33.6MB。小片子还解得开（moov 落在已写区域里，尾部填充被忽略），而 **30 分钟那一档解回来连音轨都问不到**——容器整个坏了，**而导出侧一切正常**：54000 帧全编完、泄漏 0、不抛错。这是最坏的一类失败，用户拿到放不出来的文件而软件说成功。不是"最后一块没落盘"那么简单：坏文件比真实内容还大，是补到 chunk 边界后某块的内容或偏移写错了。关掉之后 Safari 四档全过、**Chrome 反而从 12.4× 快到 13.5×**，所以不按浏览器分岔。仍满足硬规则 9。
- **Safari 没有 `showSaveFilePicker`**（实测 26.5.2：`canPickSaveFile: false`；Chrome 150 是 true），所以那边成品**只可能**走"流式写 OPFS 再触发下载"这条回退路——上面那个坏文件因此没有"也许只影响自检路径"的余地。反过来也是一条纪律：**自检永远走 OPFS**（picker 要用户手势，脚本调不起来），所以凡是"写盘路径上的结论"，都只对 `canPickSaveFile === false` 的浏览器有实测依据。这个能力位记在 `DeviceEnv.canPickSaveFile` 里，就是为了别再把一条路的结果当成两条都验过。
- **这条 bug 摆在眼前很久没被认出来**：真机自检早就记着"iOS 五档全报 16777216"，当时只当成"字节数这个代理不可信"，换成解回来量宽高就过去了。可那个数恰恰在说**文件真被撑到了 chunk 边界**。**代理坏掉的时候，别只是换个判据，要问代理为什么坏。**
- **被中断的导出会在 OPFS 里留下几百 MB，而 Safari 把由此产生的失败报成 `The operation failed for an unknown transient reason (e.g. out of memory)`**——完全看不出是存储满了。标签页被杀、或者自检判死等之后放弃那次导出，半写的成品就没人清（正常失败路径的 catch 会清）。口子有两个：产品侧是导出面板上那条 `.dlg-tidy`（**有残留才出现，清完就消失**，只在设置态量——跑起来之后目录里那个文件正是这次导出在写的，报出来会变成"你有残留要清"），自检侧是 `?autorun=clear`。两者共用 `measureExportStorage()` / `clearExportStorage()`，后者**逐个报大小**：残留文件多大本身是证据（"死等那次写到了 280MB"和"只写了 2MB"是两个不同的结论），第一版只报总字节，清场时顺手把线索毁了。
- **"导出卡住"只提示，不判失败**（`ExportProgress.stalledMs` → 导出面板的 `.dlg-stall`）。判据是 `encodedFrames` / `marker` 有没有变（心跳每秒一条，看"有没有收到消息"永远不会超时），而且**只在页面可见时计**——Safari 对后台标签的节流会让长片正常地停住好几分钟（实测同一条 30 分钟片子，前台 4.4× 跑完、后台看着像死等），自动判死等于把"导出期间切去干别的"变成导出失败。所以文案要写明"切走会暂缓、回来通常就接着跑"。同 D19 那条"看得见的降级要标注，不必禁止"。
- **取消要有宽限期，到点没回话就把 Worker 掐掉**（`CANCEL_GRACE_MS`）。Worker 卡在一个看不到取消标志的 await 上时永远不会回话，而 `export.worker.ts` 里的 `running` 还是 true——**于是这一页之后每一次导出都会被"已有导出任务在进行中"顶掉**，一次卡死污染整个会话（常驻 Worker 的代价）。原本只有 `onerror` 会换 Worker，而死等根本不抛错。不掐掉的话界面上"取消掉再试"这句话是假的。反向验证过：注入 hang → 取消 → 第一次报"已强制结束"，**第二次导出正常跑完 300 帧**。
- **同页只许一次导出，而且要在入口就拒掉**（`client.ts` 的 `activeRun`）。**常驻 Worker 让"这条消息是哪一次的"不再由生命周期免费保证**，而 `worker.onmessage =` 是单槽位：后开那次顶掉先开那次的 handler，先开那次于是再也收不到消息，而它的判据是"多久没推进"——一次**正在正常跑**的导出就被报成死等；紧接着它的看门狗发 `cancel`，把**另一次**掐了。两份读数全是假的，两边都不抛错，而我据此判出一条不存在的设备墙。所以：入口互斥（报错而不是串线）、监听器按次挂按次摘、协议里带 `RunId` 兜"上一次被放弃、它的 cancel 迟到"这种跨时间串线。**三条路都要放锁**（成功/失败/取消），否则一次失败就让这一页再也导不了——同上一条那个坑的翻版。反向验证过：并发第二次立刻报"这一页已经有一次导出在跑"，第一次的**真实错误没被盖掉**，它 settle 之后第三次照常开跑。
- **不要拿"已取消"盖掉真实错误。** `export.worker.ts` 早先是 `error instanceof ExportCanceled || canceled` 一起归成 `canceled`，于是取消前后真正抛出来的东西（`Decoder failure` 那一类）被一句"导出被取消"吃掉——而那正是要查的。现在只有 `ExportCanceled` 才算取消，取消期间倒下的照报原因并标明"取消期间失败"。同 M0 那条"两个操作数都要印在断言旁边"。
- **给导出注入 hang 验完之后必须重载页面。** 被注入卡住的那次导出留下一个**永远卡着的 Worker，而它的心跳还在跑**；同一页里再 `import('/src/export/client.ts')` 拿到的是**同一个模块实例**（同一个 specifier），于是 `sharedWorker` 被复用，第二次探测收到的是上一次那个孤儿的心跳。踩过一次：读数是"心跳有、`stalledMs` 全是 null"，看着像卡住检测没生效，其实是探测被污染了——重载后同一份代码立刻报出 9 条 4999→8999ms。注入要**按目标名开关**（只对 `hang-*` 生效），这样"第一次卡住、第二次正常"能在一轮里验完。
- **"这台机器导出快不快"要量，不要按设备分档**（`export/throughput.ts`，M3 起）。真正的变量是吞吐，而"移动端"这个标签下既有 iPad Pro 也有千元机，桌面这边也有比手机更慢的老笔记本——UA 或 `hardwareConcurrency` 分档会在快设备上白拦、在慢设备上放过去，**两种错都不报警**。做法是每次导出结束攒一个 `msPerPixelFrame` 样本（存 localStorage，因为它是**设备指标不是项目数据**，而且面板渲染要同步读得到），下次按像素量缩放预测。**样本要 ≥120 帧**——几十帧里建编码器、探编码延迟、写容器索引占大头，拿它缩放 30 分钟会离谱偏大（同"规模不对的基准量到的是固定开销"）；**跨后端不复用**（Canvas2D 和 Pixi 的每帧成本不是一个量级）；**30 天过期**；**没有样本时什么都不说**，编一个数字比沉默更坏。慢于实时只**提醒不封顶**：静默把 4K 换成 1080p 就是硬规则 10，而按预测去禁止，预测错时挡掉的是一次本来能成的导出（同 D24）。理由和放弃过的六个方案见 **D25**。
- **环境体检要把派生现象折叠回根因**（`media/preflight.ts`，M3 起）。编码器探测在环境不对时给出的是一串派生现象：非安全上下文里 `VideoEncoder` 根本不存在，于是四个编码全报"不可用"，界面上写着"这个浏览器不能编码视频"——而真实原因是页面用 `http://192.168.x.x` 打开的（`localhost` 算安全上下文，局域网 IP 不算）。**真机自检时被这串派生现象骗过，结论写成了"这台设备不支持 WebCodecs"。** 所以安全上下文不满足时**只报那一条**，并且出路要指向换 scheme 而不是"换浏览器"。**阻断（`block`，禁掉导出）和提醒（`warn`）要分开，而估算出来的结论只能是提醒**——"空间可能不够"建立在码率 × 时长上，估算偏大时阻断掉的是一次本来能成的导出。配额问不到时**整条跳过，不要当成 0**：假警告比没有警告更坏。每条都必须带出路（同 D3，纯置灰不解释就是黑箱）。决策是纯函数所以可单测，读事实那一半只是一串 `typeof x`。理由和放弃过的五个方案见 **D24**。
- **OPFS 目标在取消/失败时要自己删目录项**。`output.cancel()` 只 abort 内容，条目还在，会留 0 字节文件。picker 路径不能删——那是用户自己选的文件。

## 合成层约定（M1.6 起）

- **留边几何只有 `containRect()` 一处**，**变换摆位只有 `placeLayer()` 一处**。两个后端各算一遍就会在「预览 / 导出一致性自检」里差一两个像素，而那条断言要求黑边高度**完全相等**。
- **`LayerTransform` 相对"默认留边位置"，不是绝对画布坐标**。关键帧要表达"放大到 1.2 倍""挪到右下角"，写成绝对像素一换输出分辨率就全错。省略 = 不动。旋转用**弧度**（两个后端的 API 都收弧度），度数转换留在 UI 层。
- **恒等变换必须走"直接按 `containRect` 贴图"的原路径**（`isDefaultGeometry()`）。这不是性能优化：`translate(cx,cy)` + `drawImage(-w/2,…)` 数学上等于 `drawImage(dx,…)`，但浮点上 `(dx+w/2)-w/2` 未必精确回到 `dx`，而留边断言是逐行判黑的。改这块之后要验的不是"跑得过"，是**没用变换的输出逐像素不变**——做法见 PLAN.md 的 **D9**。
- **裁剪按比例存，而"取源片哪一块"只有 `cropRect()` 一个答案**（**D46**）。`CropInsets` 是四条边各切掉源片尺寸的**比例**（0–1），不是像素——源片分辨率会换（D37 的指认页允许"我重导了一版 720p"，尺寸不一致只警告不阻止），按像素存的裁剪那时会静默裁掉完全不同的一块画面。顺序定死**裁剪 → 留边 → 变换**，而且这个顺序有内容：**留边按裁剪之后的宽高比算**（裁成 1:1 就该在方形输出里铺满，而不是仍然带着 16:9 的黑边），变换再相对那个留边位置作用；反过来（先留边再裁）是"在输出画布上挖一块"，那是遮罩、是另一刀。可见后果是**留边方向会翻转**（横屏素材左右裁窄之后黑边从上下跑到左右），两个浏览器自检都拿这个翻转当"裁剪确实生效"的判据，因为它一眼可判。**恒等裁剪必须走整幅贴图的原路径**（`isDefaultCrop`，同 `isDefaultGeometry`）：`drawImage` 的 9 参数形式即使给的是整幅源片，采样路径也未必逐字节相同，而留边断言是逐行判黑的。
- **裁剪不是可动画属性，也不进批量面板**（D46）。这是**范围决定不是原理决定**（对比 `speed`：那个有原理上的理由——速度关键帧会让源片时刻变成速度曲线的积分）。裁剪动画就是"推近"，而缩放 + 位移已经能表达且本来就在关键帧里；加四条边意味着 `Record<AnimatableProperty, …>` 那两张表各加四项、关键帧轨多四行、批量面板多四行。将来要加是纯加法。**对边加起来到 100% 时拒绝、不夹紧**（夹紧要么改用户刚输的那个数、要么改另一边，都是"选了 A 拿到 B"），单边那个 0.9 的上限只是给输入框一个头。**只有画面轨上的片段能裁**，音频轨上存下来永远不会被求值，是 D19 那类"存了但不生效"——而这道门要写在**纯函数里**，不能只靠界面不显示。
- **Pixi 的槽位判据要把"取样矩形"整个记下来，不只是源尺寸**（D46）。换 `Texture` 的 frame 和 `ImageSource.resize()` 踩的是同一块地（D36）：**都通知不到 `Sprite` 的包围盒**。记一个矩形而不是"源尺寸 + 裁剪"两组判据，是因为源尺寸变了矩形也会变，一个判据把两种情形都盖住，也就不会出现"改了源尺寸判据、忘了改裁剪判据"。缩放的分母也要换成**裁剪后**的尺寸（sprite 的纹理就是那一块，拿源片全尺寸去除会让裁过的图层只画出该有的一半大）。注入验过三种失效签名，见 `compareCrop` 的文件头。
- **"这一层长什么样"由 `layerLooks()` 一处给出**（`edl/sampling.ts`，D46）。它有两个调用点——导出的 `pipeline.ts` 和预览的 `preview-engine.ts`，而它们必须给出同一份字段。原来那三行（`transform` / `color` / `lut`）是各写一遍的，加第四个字段时漏掉一边的表现是**预览里裁过、成片里没裁**，两条路径都不抛错。收成一处之后这类分叉在结构上不可能，同 `containRect` 那条"只有一处"。**条件展开不能改成直接写 `crop: undefined`**：`exactOptionalPropertyTypes` 下那是另一种类型，而后端靠"没有这个字段"走恒等快路径。
- **Pixi 的槽位换源尺寸时要换一张新 `Texture` 塞给 sprite**（D36）。`ImageSource.resize()` 会把 `Texture` 的 frame / orig / uvs 全部更新好（单独量过，全对），但**通知不到 `Sprite` 的包围盒**——sprite 还按上一个源的尺寸算自己有多大，而我们给的 `scale` 是按新尺寸算的，画出来就是"旧尺寸 × 新缩放"。实测先画 640×360 的帧再画 200×100 的图：屏幕那 320px 宽只显示了图片左边 100px（640/200 = 3.2 倍），纵向拉到底，**而上黑边仍然是对的 80px**。**这不只影响图片**：同一条轨上接两个分辨率不同的视频片段就会中招。它也**不可能**被"预览 / 导出一致性"抓到——两条路径都走 Pixi、各画各的同一个错，判据是 Canvas2D 后端同一份输入完全正确。判据要记在**我们自己这边**（`slot.sourceWidth`）：第一版拿 `texture.frame.width` 当判据，而它在 `resize()` 之后**已经等于新尺寸**，条件恒为假、那段代码从没执行，改完读数一个字节都没变。
- **图片素材的解码要"先备好、再渲染"**（`compose/image-store.ts`，D36），纪律与字体注册**一字不差**：`composeFrame()` 是同步的而 `createImageBitmap()` 是异步的，所以**导出在逐帧循环之前 `prepareImages()`**、预览按需解（没解好的那一帧先不画，同 video 元素的 `readyState < 2`）。两条路径的**错误处理刻意不同**，这不违反硬规则 2——像素来自同一个 `createImageBitmap` 和同一份文件：预览里"还没解好"是常态，导出里那是 bug，而它的表现是**成片里少一层画面且报成功**。解码尺寸按输出分辨率的 **2 倍**封顶（一张 6000×4000 解出来是 96MB，同 D13"按字节限流不按条数"），而**缩过要在检查器里说出来**——那是一次画质取舍，静默缩掉就是硬规则 10。
- **Pixi 后端的 sprite 是跨帧复用的，`anchor` / `rotation` 两条分支都要显式写**。带变换的帧把 anchor 设成 0.5，下一帧走恒等路径时若不复位，整层会偏半个图层——且只在"某帧有变换、下一帧没有"时出现。
- **文字只栅格化一次**（`compose/text-raster.ts`），两个后端都当普通 `{kind:"image"}` 图层画。两边各写一套文字渲染就是硬规则 2 的新入口——字形/断行/描边宽度会不一样，且不报错。栅格画在**输出尺寸**的透明画布上（于是 `containRect` 退化成 1:1，默认摆位天然正确），文字在里面恒定居中，**位置和大小一律由 `LayerTransform` 表达**。字号/描边/阴影都存"占输出高度的比例"，不存绝对像素。完整决策见 PLAN.md 的 **D11**。
- **自定义字体必须在预览和导出**两个上下文**各注册一次**（`compose/font-registry.ts`，D31）。`FontFaceSet` 每个上下文一份，主线程 `document.fonts.add()` 不会让 Worker 看见——而没看见时 `ctx.font` **静默回退**到兜底字体：预览里是用户选的字，成片里不是，两边都不抛错。纪律是**先注册、再进 EDL**（导入时 `await face.load()` 成功才 `addFont`；恢复时在 `loadProject()` 里读完字节就注册，装不上的当"拿不回来"；导出在逐帧循环之前注册），于是"EDL 里有的字体本上下文一定注册过"是结构性的，而 `rasterizeText` 对没注册过的族名**抛错**——那道断言只会在有人新开一条渲染路径时开火。
- **族名由我们生成（`KerfFont-<时间戳>-<序号>`），而且它同时就是这个字体在项目里的 id。** 不给片段另加一个 `fontId`：`TextStyle.fontFamily` 本来就在，两个真值来源"都设了听谁的"错了不报错，只表现成"选了 A 拿到 B"。不取字体文件里的名字：同名不同版本会互相顶掉，表现为"改了一个片段的字体，另一个也跟着变了"。前缀的用处是**分得清哪个族名要我们注册**——系统族名交给浏览器解析，`KerfFont-…` 没注册过就必须抛。
- **`FontFaceSet.check()` 不能当"我的字体生效了没有"的判据。** 实测（Chrome 150 / Safari 26.5.2，主线程与 Worker 四种组合）它对一个**根本不存在**的族名同样返回 `true`——匹配不到任何 face 时按"系统字体兜着"算成真。拿它当判据就是把一个恒为真的量当读数（同那条"心跳固定带 `audio`，光靠它定位不到"）。唯一有效的判据是**量宽度**（`fontEffective`），而它**只当诊断不当闸门**：用户导入一个与兜底字体度量相同的字体时它会误判，而那种情形下输出本来就一样。真正的闸门是三件不会误判的事——有没有 `FontFaceSet`、字节是不是字体（`load()` 会抛）、族名有没有注册过。
- **字体的字节在 EDL 里存原始字节，不存 `File`，也没有"解析结果"可存**（对比 `LutSource`）。快照里要把 `data` 剥出去单独存（同 `File`）：快照每次编辑都重写，而一个 CJK 字体动辄 10–20MB。字体找不回来时**必须清掉 `style.fontFamily`**（LUT 是"可以清"，这里是"必须清"）——留着渲染就抛，表现成"恢复完预览就崩"。
- **栅格缓存按字节限流，不按条数**（`compose/raster-cache.ts`）。踩过：按 32 条卡，而每张栅格是**输出尺寸**的画布，1080p 下单张 7.9MB，32 条就是 253MB；条数上限对分辨率一无所知，而会爆的是字节。现在是 32MB 预算 + LRU + 保底 2 张。淘汰用 LRU 而不是"满了整体清空"，也不用"调用方每帧报可见列表"——后者是个忘了调也不报错的接口。理由见 **D13**。
- **改断行要跑 `text-raster.test.ts` 的 23 项**。踩过的两个坑：逐字符判断"能不能断"在拉丁词中间超宽时退不回上一个空格，整个词会溢出；避尾只做"不在这里断"会让开括号卡在行中间而行照样超宽，必须**把断点前移**。切分用 `Intl.Segmenter` 按字素簇，不是码点——码点保得住代理对但保不住 ZWJ 合成的 emoji。
- **shader 转场是 `ComposeLayer` 上的双输入节点**（`{kind:"transition", from, to, progress, effect}`），两个输入各渲进一张**输出尺寸**的渲染目标再跑双采样器 shader。`from` / `to` 在**类型上**只能是单层、不能再套转场——那是把 D19 的"一帧最多两层参与转场"钉进类型里，能嵌套的话渲染目标数量就没有上界。节点自身没有 `transform`：效果作用在**输出空间**，所以画中画层做擦除时擦的是整屏。理由和放弃过的四个方案见 **D20**。
- **不要用 Pixi 的 `Filter` 管线做双输入**。filter 会把内容先渲进一张带 padding 的临时纹理、再靠 `uInputSize` / `uOutputFrame` 换算 UV，UV 与屏幕不是 1:1；而每种效果都按屏幕坐标定义，参照实现要算得准就必须自己给几何和 UV（自建 `Geometry` + `Shader` 的全屏 `Mesh`）。
- **转场的混合函数同样有一份 CPU 参照**（`compose/transition-shader.ts` 的 `mixTransition`），GLSL 与它**逐行对应**，羽化宽度和故障的三个常量都只有一份定义且 GLSL 从 uniform 读。这是第三份（前两份是 `colorMatrixOf` 和 `sampleLutTexture`）。**spike 里两个输入用纯色**：那把被测对象从"采样精度"里剥出来，只剩混合函数本身。
- **shader 里不许出现精度是实现定义的函数，只要它的结果会决定"取哪一层/哪个纹素"**（D30）。`fract(sin(x)*43758.5453)` 那个一行哈希在 GPU 和 JS 上不逐位相同（`sin` 精度实现定义，而乘 43758 把 1e-7 放大成 4e-3，`fract` 再把它变成无关的数），于是 GPU-vs-CPU 断言只能给一个松到抓不住东西的容差——**这一条把故障转场从 M2 中段拖到了最后**。现在用 **PCG 整数哈希**：只有 mod 2³² 的乘法、移位、异或。同理故障的抖动幅度用抛物线 `4l(1-l)` 而不是 `sin(lπ)`，因为幅度决定取样落在哪个纹素上。
- **哈希归一化只取高 24 位再除 2²⁴**（`hashUnit`）。`float(uint)` 在超过 2²⁴ 时要舍入而 JS 那边是双精度，直接 `h / 2³²` 会差约 1e-7——单独看无所谓，落在"这条带翻没翻"的比较上就是一个完全不同的像素。
- **纯色层验不了位移**。故障有两半：取哪一层（哈希）和取哪一列（位移）。纯色层在哪儿取都一样，所以位移那一半要另开一组**两色层**用例，取样点放在分界附近、距分界大于过滤半径小于最大位移。反向验证过：把 GLSL 里的位移整个丢掉，只有那一组红（140/255）。
- **擦除边界要走完 `1 + feather` 才算到头。** 第一版写成 `t*(1+feather) - feather/2`，两端各差半条羽化带，表现是**转场首尾几帧画面纹丝不动、结束时角落还留着上一层的残影**——残影只有几个像素宽，很容易当成编码噪声。单测里"t=0/1 时整屏是纯的"钉的就是这条。
- **转场的时间模型只有 `src/edl/transition.ts` 一处**（窗口在哪、进度多少、余量够不够），纯函数、只认帧号、和渲染方式无关。窗口**恒以交界为中心对称**（所以恒为偶数帧），**每个片段最多借出自己长度的一半**——后者让"一帧最多两层参与转场"成为结构性保证，reader 的游标数和预览的 video 元素池都按这个上界设计。**交叉溶解不需要新的合成能力**：入场层画在出场层之上、alpha = 进度，走的是既有的 `LayerTransform.opacity`，所以它不进 `supportsEffects` 闸门（shader 转场才会）。理由见 **D19**。
- **余量不足时定格边缘帧，不拒绝创建，但必须把定格帧数报到界面上**（`frozenFrames` → 检查器）。最常见的场景恰恰一帧余量都没有：两段满长素材前后相邻。它和硬规则 10 那种"静默降级"的区别是**定格在预览里看得见**——看得见的降级要标注，不必禁止。
- **起播余量必须同时出现在漂移判据里**（`START_LEAD_SECONDS`，D34）。原点定在"锚定那一刻 + 50ms"好让第一段有时间混出来，但漂移判据里不减掉它的话，声音天生就恒定晚 50ms——吃掉 80ms 容差的六成，只剩 30ms 给帧号取整（±16.7ms）和时钟抖动，于是**正常播放时每秒重新对齐一次**（实测 125ms 就越界，之后被 `MIN_RESYNC_INTERVAL_MS` 限流成 1 次/秒）。而重新对齐要重建 AudioContext 和混音器（重探解码器，几百毫秒），表现是声音每秒一顿、听起来像素材本身有问题。这就是 D22 记着的"AudioContext 风暴"的限流版：没崩，只是一直难听。护栏是 M0 那条"采集期间没有重新对齐"。
- **不要 `await context.resume()`**（D34）。实测 Chrome 150：没有用户手势时它返回一个**永不 settle** 的 promise（`state` 停在 `suspended`、`currentTime` 一动不动、既不 resolve 也不 reject）。await 它就等于 `startAt` 永远不返回：session 建不出来、`start()` 的 promise 也不 resolve，而 tick 里那套"试几次就放弃"的闸门**根本轮不到执行**；走 tick 重排那条路时 `restarting` 还会永远停在 true，这一页从此不再出声。
- **预览出声播的是导出那份 PCM，不是自己再混一遍**（`preview/audio-engine.ts`，D26）。跑同一个 `createMixer`、把产出的段排进实时 `AudioContext`，所以淡化曲线 / 等功率还是等增益 / 静音轨处理**不可能分叉**。段长压到 1 秒（导出是 10 秒）——起播延迟等于混第一段的时间。声音是**被动跟随主时钟**的一方，同 video 元素的漂移纠正；音量和静音只影响预览，不进 EDL。踩过两个都不抛错的坑：**context 停在 `suspended` 时不能谈漂移**（时钟不走，重排一万次也对不齐——实测 2.4 秒建了 25 个 AudioContext，而 Safari 对同时活着的有硬上限），以及**原点必须在第一次 `tick` 里锚定**（建完混音器就地锚会晚一个"探解码器的耗时"，于是漂移从第一帧起就成立、每秒重排一次）。**给错误加限流会把"风暴"变成"周期性抽动"，看起来像修好了**——读数恰好等于限流值本身就是"还在持续触发"的信号。
- **变速播放的判据只在 `preview/shuttle.ts`，而播放状态是一个有符号倍率**（**D49**）。0 暂停 / 1 常速正放 / 负数倒放，`playing` 从它派生——存"布尔 + 数字"两个状态能构造出"playing 为真而倍率是 0"和"倍率是 4 而 playing 为假"，表现分别是"播放中画面纹丝不动"和"暂停了播放头还在跑"。**倍率每帧传进 `renderLive` / `startPlayback`，不许记在引擎里**（第二个真值来源，不同步的表现是"松开 L 之后画面还在 4× 跑"）。**声音只在常速正放时出现**（`isNormalPlayback`）：预览播的是导出会写进成片的同一份 PCM（D26），2× 上出声就得混一份导出永远不会产出的字节。**"要不要出声"和"要不要报读数"是同一个判据的两面**，写成两个谓词会漂，漂了就是"静音了但界面没说"（硬规则 10）。**变速时一次都不能 `audio.tick()`**——`invalidate()` 之后它会自己拿播放头重排，于是"变速中编辑一下"会让声音在 4× 上原速响起来。
- **video 元素的 `playbackRate` 是三个因子相乘，而它为 0 时漂移容差必须让路**（`elementPlaybackRate`，**D49**）。变速倍率 × 片段速度（D39）× 定格与否（D48），**负数取 0**（没有浏览器实现负的 `playbackRate`，所以倒放只能逐帧 seek）。三个因子漏一个都不报错：漏倍率是 8× 时元素仍以 1× 走、被漂移纠正每帧拽一次；漏片段速度是变速片段每 3 帧被拽一次（这条**从 D39 起就在**，靠容差兜着没人认出来）；漏定格是**播放中的定格片段并不定格**（实测游走 3.03 源片帧 = 那条容差本身、1 秒 10 次 seek）。**容差和"元素自己在走"是配对的**：容差挡的是"它正走过去，别打断它"，而 `playbackRate` 为 0 的元素永远走不到新位置，留着容差的表现是倒放时画面每 3 个源片帧才动一次。判"这一帧要不要再 seek"用 **`SourceHandle.wanted`（上一次要求它停在哪）不用 `currentTime`**——后者 seek 完会吸附到帧边界、和要求值差一点，于是"要求没变"永远判不出来，定格片段会每帧白发一次 seek；而 `wanted` **必须由每一处写 `currentTime` 的地方一起更新**（`seekElement` 是唯一入口），漏更新的形态是"scrub 到别处再按播放，画面停在 scrub 的位置不回来"。**换档不重走 `startPlayback`**（那要 await seek + play，是一次多余的停顿），代价是 `renderLive` 要自愈：元素该走却停着时补一句 `play()`。
- **关键帧求值在 `src/anim/keyframes.ts`，全程只用帧号，秒进不来**（所以它连帧率都不需要）。帧偏移**相对片段起点**，不是绝对时间轴帧号——因此裁入点时要平移关键帧。`resolveTransform` 在没有属性被动画时**必须原样返回 `base`（包括 `undefined`）**：返回 `{}` 会让所有静态图层集体掉出上面那条恒等快路径。理由和放弃过的方案见 PLAN.md 的 **D10**。
- **选后端只有 `compose/backend.ts` 一处**（`createCompositor`）。预览和导出必须画得一样（硬规则 2），两边各写一遍 `try pixi catch canvas2d` 迟早会分叉，而分叉不报错——今天只是留边差 1px，接了滤镜就是"预览有效果、成片没有"。退回 Canvas2D 时 `backend` 会一路报到导出面板上。
- **一级调色的算法只有 `compose/color.ts` 一处**（`colorMatrixOf`），四个量编译成一个 5×4 矩阵，Pixi 后端只负责搬。编成矩阵而不是写四段 GLSL 是为了**同一份算法能在 JS 里跑**（`applyColorMatrix`）——于是语义可单测，而且 spike 能断言「GPU 出来的像素 == CPU 参照实现」。shader 写错不报错：预乘 alpha 上算、偏移列当 0–255、行列转置，三种都画得出图。语义对齐 CSS filter（倍数 1 = 不变、色相用弧度、sRGB 空间），**顺序定死为色相 → 饱和度 → 对比度 → 亮度**。理由见 **D17**。
- **恒等调色必须走"不挂滤镜"的原路径**（`isDefaultColor`），和恒等几何完全同构（D9），也同样不是性能优化：挂上 filter 会让 Pixi 先渲进临时纹理再合成，多一次重采样。**滤镜和 sprite 一样是跨帧复用的槽位状态**，恒等分支要显式把 `filters` 清成 `[]`——不清就会把上一帧的调色画到这一帧头上，且只在"某帧有调色、下一帧没有"时出现。
- **Canvas2D 做不了这些效果，但不能静默丢**。`Compositor.supportsEffects` 为 false 时 `composeFrame` 照常画、只忽略 `color` 和 `lut`（跑到第三千帧才抛错更坏）；守卫在界面上——`observedCapabilities()` 告诉导出面板本机能力，用了效果而做不了就**禁掉导出并写明出路**（同硬规则 10、同 D3 的做法）。**不要**用 `ctx.filter` 在 Canvas2D 上拼一个近似版：那会让同一份 EDL 在两台机器上画出两张画面。它是**一个布尔而不是一组能力位**——调色和 LUT 都要 WebGL、都是后端级别的有或没有，拆开只会造出一堆恒等的标志。
- **LUT 在 EDL 里存解析结果、不存文件**（`Timeline.luts`，片段用 `lutId` 引用）。存文件意味着预览和导出各解析一遍，是硬规则 2 的新入口。查表算术同样有一份 CPU 参照实现（`sampleLutTexture`），spike 拿它和 GPU 对拍。铺法是**一行 N 个切片的 2D 图**（`N*N × N`，切片序号=蓝、片内 x=红、y=绿），所以**尺寸上限 45**（宽度是 N²，而 WebGL2 只保证 2048）；超了明确报错，不截断。**顺序定死：先一级调色，再 LUT。** 理由和放弃过的方案见 **D18**。
- **每加一种 GPU 效果，都要同时加一份能在 JS 里跑的参照实现。** 「预览 / 导出一致性自检」覆盖不了 shader 本身——两条路径共用同一个（错的）shader 时它是绿的。实测过：把 LUT 的半纹素偏移改错，第二个自检全绿，只有 spike 里那条 GPU-vs-CPU 会红。
- **先确认"健康值"量的是被测对象，而不是量法自己的偏差。** 交叉淡化的包络断言第一版健康值 0.033、最轻的破坏 0.076，两边余量都很小，看着像"这个量就是这么糙"。实际上三分之二来自 `rmsAround` 漏了跨窗口起点的那一整个解码包，于是量到的区间偏后——**包络正在下降时这表现为"值偏低"，和真实误差长得一模一样**。用 21 点密集扫描单独把真实包络量出来（只差 0.010）才把两者分开，修好后健康值 0.008，容差得以从 0.05 收到 0.03。**容差定不下来的时候，先怀疑量法。** 这条在做配乐时**一轮里咬了两次**（D35）：一是那条起始沿断言第一版拿**理论值**（第 30 帧 = 1001ms）当判据，红了 67.9ms ≈ 20（我刻意留的静音前导）+ 47.9（素材自己的 AAC priming，2112/44100）——**一个完全正确的产品被判成错**，而"两个操作数都要印出来"那条注释里早就写明了要量素材、别拿理论值；二是真机验波形时读到峰值只有 0.086，差点去查波形代码，而用 ffmpeg `volumedetect` 量素材本身是 **−21.3dB = 0.0862**，与波形报的 0.08575 完全对上——是 ffmpeg 的 `sine` 滤镜本身不满幅。**凡是"这个读数看着不对"，先量一遍被测对象之外的那一半。**
- **"两条红是同一个根因"往往对，但别停在第一个能解释它们的假设上。** iOS 上 M0 同时红两条：音画偏移 9.3ms、交叉淡化包络偏 0.037。按曲线斜率反推，六个包络取样点给出**一致方向**的时移（淡出偏高、淡入偏低 = 包络整体偏晚），和 9.3ms 同量级——同根因这一步是对的，**但我据此判成"产品把音频挪晚了"，错了**。逐段并排量下去（素材读回 / 混音输出 / 裸编解码 / 封装往返 / 成片三个提示音）**两个浏览器分毫不差、成片偏移全是 0**；差异只在最后一步的读法：同一个文件，从 0 读 1041.0ms，**seek 到 0.5 秒再读 1050.4ms**，而 Chrome 两种都一样。是自检的测量函数踩了 Safari 的 seek 时间戳，不是产品。**判据是把"量法"和"被测对象"分开各测一次**——做法就是只改一个变量重跑（这里是"要不要 seek"）。
- **跨浏览器只红一家时，先在桌面上复现同一个引擎。** 那 9.3ms 在**桌面 Safari 上和 iOS 分毫不差**（同一套 AudioToolbox），于是整个定位过程不用再借手机——真机往返一次几分钟，桌面一次十几秒。手上没有真机模拟器时这条尤其值钱。
- **别把"某个浏览器读到的时间戳"当成事实。** 自检里量音频一律**整轨从 0 解一次**再按绝对样本位置索引（`decodeTrackToPcm`），不要 seek 到关心的那一段再靠 `audioSample.timestamp` 定位。产品侧 `mixdown.ts` 的 `decodeRange` 仍是"seek + 按时间戳摆位"，实测它在 Safari 上是对的，但和被修掉的那两个函数踩在同一块地基上——动它要配自己的验证。
- **断言红了但没有诊断，等于只知道"坏了"。** 上面那条能查下去，靠的是把**两个操作数**都印在断言旁边（素材 4044.0ms − 入点 3003.0ms = 期望 1041.0ms · 成片 1050.4ms），而不是只印差值——只印差值时"素材那边量歪了"和"成片真被挪了"长得一模一样。凡是"两个读数相减得出结论"的地方，两个读数都要报出来。
- **取样点要让参照实现自己挑，不要手写。** 故障那条"不同的带给出不同像素"第一版手挑了第 0 / 7 / 15 条带，结果三条在 t=0.5 时**恰好都已经翻过**（每条带翻转与否各 50%，三条同侧的概率 1/8），断言假红。现在由 `glitchPoint()` 扫一遍挑出"一条已翻、一条未翻"，挑不出来就抛错——那说明常量被改得所有带同时翻，这一组从此是空断言。位移那一组同理（要挑 `|位移| > 取样点到分界的距离` 的带）。这是"挑用例时要先问被测的那个量在这里是不是恰好等于零/一"的一般形式：**凡是取样点的有效性依赖被测代码算出来的数，就让被测代码去挑，并且挑不出来要报错。**
- **同一条的又一个形态：被乘以零的那个因子测不到。** 配乐自检第一版只放了一个 `sourceIn: 0` 的片段，而 `sourceMicrosAt` 是 `frameToMicros(sourceIn, 栅格) + …`——**`sourceIn` 为 0 时栅格被乘以零**，于是那条起始沿断言一次都没碰到 `sourceGridFps`，把栅格改成固定 24fps 仍然全绿。补了第二个**裁过入点**的片段才抓得住（Δ −545ms）。判据：**看一眼被测的那个量出现在哪个乘法里，另一个因子是不是 0 或 1。**
- **"GLSL 里不许出现 X" 这类断言必须先剥掉注释。** 第一版直接搜整份 shader 源码，被 shader 里那句「不要换回 `fract(sin(x)*…)`」的注释命中——一条警告不要用它的注释，被当成了用了它。
- **注入之前先问"这真的是个 bug 吗"。** 验音量包络时我把 `crossfadeGainAtFrame` 里那层 `Math.min(toProgress, …)` 去掉当破坏，两层断言全绿，一度以为又是断言没牙。查下去发现**那层夹紧可证明冗余**：`crossfadeGain` 内部已经把进度夹在 [0,1]，而 `toProgress < 1` 只在窗口被区间切掉尾巴时出现，那时 `lastFrame` 同样被切到那里、帧号到不了更远。也就是说**我注入的不是缺陷，全绿是正确答案**。换成"忘了把淡化折进合成曲线"（让它只返回 `baseGain`）才是真缺陷，M0 当场红到 1.15e-1。**一次"注入了却没红"有两种解释——断言没牙，或者你注入的东西根本不改变行为——不要默认是前者。**
- **反向验证不只是"确认断言会红"，还要确认它红的是不是你以为的那一条。** 音量那两条断言（无转场 / 带淡化）我写的理由是"带淡化那条每个片段都有 `ramps`、进不了恒等快路径，所以只有无转场那条能抓快路径的 bug"。注入那个 bug 之后**两条都红了**——因为 `renderSegment` 的排期是**按段重算**的，段长 0.5 秒而转场窗口只有 0.667 秒，绝大多数段里 `ramps` 是空的。两条断言各自的价值不变，但"这条覆盖了什么"**光读代码是会推错的**。同一轮里还栽过一次更直接的：第一版"音量不乘进 `baseGain`"的用例整条导出，而入场片段在窗口起点的 `baseGain` 恰好是 **0**，`0 × 0.25` 还是 0——注入破坏后断言全绿。改成让导出区间从窗口中间切过去（`baseGain` = 0.7071）才抓得住。**挑用例时要先问"被测的那个量在这里是不是恰好等于零/一"。**
- **容差要落在"坏掉时的值"和"实测健康值"之间，不能贴着后者。** 转场的混合比例断言第一版取 30，而"把溶解整个丢掉"这个破坏在交界前一帧只造成 32 的偏差——余量只剩 2，等于把断言交给运气；健康值实测是 1，改成 10。**这个数只能靠反向验证定**：不做破坏就只知道健康值，不知道该留多少余量，于是要么定得太松（抓不住）要么太紧（误报）。
- **给 shader 做的探针要挑能把误差放大的参数，不是"真实"的参数。** 半纹素偏移写错时误差与 LUT 格点间距成正比：17³ 上只差 4/255（刚压在容差线上），5³ 上是 17/255。自检里那张恒等 LUT 因此刻意用 5³。用真实尺寸去测一个亚纹素级偏移，等于把断言的余量交给运气。
- **一张画布只能有一种上下文类型，而且 Pixi 销毁过渲染器的画布不能再用。** `renderer.destroy()` 会 `loseContext()`，之后在同一张画布上再 `init()` 会**死循环**（实测 Chrome 150，标签页 100% CPU，不抛错、不报警）。两条推论：**改分辨率要走 `compositor.resize()` 就地改，不能销毁重建**；**预览引擎自己建画布**（`createPreviewEngine(container, …)`），不接受外面传进来的——只要画布是外面的，React 严格模式的双调用、改分辨率、热更新迟早会撞上这个死循环。从 Canvas2D 迁过来最容易踩这条：那边"dispose 再 new 一个"完全没问题。
- **自检里量预览画布要先 `drawImage` 到一张干净的 2D 画布上再量**，不能对引擎画布直接 `getContext("2d")`——接了 Pixi 之后那是 WebGL 画布，那句返回 null。
- **`pixi-compositor.ts` 对 pixi.js 只有 `import type`**，实例走函数里的动态 `import()`。所以谁静态 import 它都不会把 Pixi 拖进自己的 chunk——这和 mediabunny 的"文件边界"模式不同，靠的是**异步工厂 + 同步 `composeFrame`**。
- **Pixi 后端里临时 `VideoFrame` 必须在 `render()` 之后才 `close()`**。纹理上传发生在 render 期间；Canvas2D 的 `drawImage` 是立即的，所以那边"画完就关"是对的。从 Canvas2D 迁过来最容易踩这条，且不报错。
- **每个图层一个常驻 `ImageSource`，逐帧只换 `resource` 再 `update()`**。每帧 `Texture.from(frame)` 会逐帧新建 GPU 纹理，导出慢一个量级。自检里"GPU 纹理数不随帧数增长"就是锁这条。
- **WebGL 上下文有预算，超了浏览器驱逐最老的那个——所以危险的是长命的那个，不是新建的。** 直觉会先去防"连着导出十几次，下一次起不来"，但那个方向天然安全；真正会死的是**预览**——它从打开项目起就一直握着一个合成器，是全场最老的。Safari 上每轮新建时 12 轮就把它驱逐掉，**而且救不回来**（被预算驱逐的上下文 `recover()` 会超时：浏览器要等预算腾出来才还）。所以**导出侧必须复用常驻合成器**（`pipeline.ts` 的 `acquireCompositor`），Worker 也跟着跨导出常驻（`client.ts` 的 `sharedWorker`）——已落地，spike 断言的就是这个架构。
- **上下文丢失要能恢复，而重建 GPU 状态的是 Pixi 不是我们**（`recover()`）。三件事缺一不可：**丢失事件上必须 `preventDefault()`**（否则 `restoreContext()` 是空操作、`webglcontextrestored` 也不派发，三种组合都实测过）；**扩展要在创建时就抓住**（丢失后 `gl.getExtension()` 返回 null，"用时再取"永远取不到，踩过一次，表现为 recover 什么也没干就超时）；**别自己锁一个 `contextLost` 布尔量**，判据要始终问 `gl.isContextLost()`，只置位不复位会让"救得回来但我们不让它活"。事件在 OffscreenCanvas 上**会**派发（实测 Chrome），但仍然轮询兜底。
- **测吞吐要在一个 WebGL 上下文都没建过的时候测。** 带着残留上下文量到的是 GPU 内存压力，不是导出的常态。spike 里吞吐是**第 0 步**，别把它挪到后面去。
- **Pixi 相对 Canvas2D 的耗时比值是"记账"，不是卡口。** 滤镜 / LUT / shader 转场在 Canvas2D 上根本做不了，所以"慢"的对立面不是"快"而是"没有这个功能"。那条断言（`SLOWDOWN_LIMIT = 2.5`）只抓数量级的结构性错误，已知的那一类是每帧 `Texture.from()` 新建纹理。实测 Chrome 0.93× / Safari 1.69×。
- **锁 WebGL，不要 WebGPU**，也不要关 `preserveDrawingBuffer`（实测开销为零，见 PLAN.md §7 M1.6）。
- **Pixi 后端已经接在预览和导出上了**（2026-07-27，D16）。`compose/pixi-compositor.ts` 仍然只被 `backend.ts` 引用，换/加后端从那一处改。

## 四个自检，改到相关地方就得跑

都在「M0 自检」面板里，`pnpm dev` 后从编辑器顶栏进。前三类错误**都不会报错**，只会静默产出错误的片子，单元测试覆盖不到。

| 改了什么 | 必须跑 |
|---|---|
| 导出管道 / 时间基 / 音频 / **预览出声** / **纯音频素材** | **M0 自检**（83 项）：生成素材 → 探测 → 导出 trim 区间 → 读回断言。「导出没有移动音频」比对**素材与成片里第一声的位置差**，抓的是编码延迟没补偿那一类音画偏移，旁边两条是**诊断行**（实测出来的延迟样本数与相关性、以及相减得出结论的那两个读数各自是多少）——只印差值时"素材那边量歪了"和"成片真被挪了"长得一模一样，而 Safari 那次正是前者；后 9 项是**音频交叉淡化**，另导一条三片段的音轨、读回来量增益包络，两条曲线各在 3 个进度上对 `crossfadeGain()` 的参照值，外加两条"包络确实在变化"；最后 12 项是**分段混音**（分段与不分段逐样本对拍，无转场 / 带淡化各一组，另有"真的切成了多段""参照只有一段""PCM 里有信号"三条护栏；以及峰值不随片长增长、峰值确实采在段内）；最后 8 项是**片段音量**（判据都是"音量 0.5 时逐样本恰好等于原值 × 0.5"）：**静态**音量在无转场 / 带淡化两条时间轴上各一组，实测最大差 **0.00e+0**、峰值 0.253 → 0.127；**恒定包络**同样两组，走的是 `gainCurve` 那条分支（淡化被逐帧重建再乘音量），带淡化那组实测 **3.73e-9**（f32 曲线的精度地板）、无转场那组仍是 0——这两条是 `crossfadeGainAtFrame` 唯一的端到端护栏，反向验证过：把它改成只返回 `baseGain`（= 忘了折进淡化）时带淡化那组红到 1.15e-1 而无转场那组仍绿；最后 11 项是**预览听到的 == 成片里的**（D26 欠的那条，**D34** 补上）：把预览那个 `AudioContext` 的输出用 AudioWorklet 无损抓下来，和 `createMixer` 产出的同一份 PCM 逐样本对拍，实测**最大差 3.02e-16**（float64 噪声）。另外五条是量法自证——起始沿要落在**起播余量**上（只判"是个合理的数"时整体平移会被对齐吸收掉，实测过）、**错开一个样本必须显著不一致**（3.55e-2，同时证明对齐唯一且不是在比静音）、两边都要有信号、worklet 没失联、以及**采集期间一次都不许重新对齐**。这一条要用 `--autoplay-policy=no-user-gesture-required` 起浏览器：没有用户手势时 `AudioContext` 起不来，而那时读数是"两边都静音、最大差 0"——不加护栏就是一条恒绿的假断言。**但那个 flag 不够**：从脚本后台起、窗口没到前台时，同样抓到静音（实测报 `峰值 0.0000`、起始沿 `采集第 -1 个样本`），于是**总数从 67 掉到 64**（三条依赖"有信号"的自动跳过）**并红 2 条**。这两条红长得就像刚改的代码坏了——判据是**把改动 stash 掉用同一套环境再跑一遍**（实测改动前后逐字段相同：都是 64/62、都是 `-2115`），同那条"先确认量法量的是被测对象"。所以纯粹要验取帧 / 合成时跑「多片段一致性」和「预览一致性」更干净，它们不需要 `AudioContext`；最后 8 项是**纯音频素材（配乐）**（**D35**）：画面素材刻意不带音轨，于是成片里每一点声音都只能来自配乐。核心判据是**成片里的第一声 − 片段被放的位置 == 素材里对应位置之后的第一声 − 那个位置**（两边同口径，实测 Δ **0.0ms**，容差 5ms），两个操作数都印在旁边。**两个片段**：一个整段放进来（钉"落在第几帧"），一个**裁过入点**（钉帧栅格换算）——只有前者时把栅格改成固定 24fps 仍然全绿，因为 `sourceIn` 为 0 时栅格被乘以零。配乐用 `beeps` 而非 `tone`（连续音上"取错源片位置"测不出来），且 `sourceIn` 必须落在两声之间的**静音**里 |
| 预览 / 合成 / 文字 / 调色 / LUT / **图片** / **裁剪** | **预览 / 导出一致性自检**（26 项）：同一帧两条路径逐像素比对；**带关键帧的变换**在 3 个取样帧上逐帧比对摆位（并断言摆位真的在变——变换被整条丢掉时"两条路径一致"仍然成立）；**文字层**的分区测量（D6）；**一级调色**四条（一致 / 真的生效 / 落在 `colorMatrixOf` 的参照值上 / 没顺手挪动几何）；**LUT** 三条（一致 / 通道真的轮换了 / 不改几何）；**图片层**四条（**D36**）——两条路径都画出来了 / 留边逐条一致 / **摆位命中手算的 `containRect`**（2:1 的图在方形输出上应上下各留 80px，只比"两条路径一致"抓不住它）/ 没有左右翻转（探针图左红右蓝，纯色而非渐变，把被测对象从采样精度里剥出来）；**裁剪层**三条（**D46**）——两条路径的留边逐条相同（漏传 crop 会红）/ **确实生效**（判据是留边方向从上下翻到左右，一眼可判）/ **命中手算值**（算式的输入必须是**源片**尺寸不是输出尺寸，第一版拿 OUT_SIZE 当源片宽度，期望算成 80 而实测 18）。**这三条的分工验过**：`layerLooks` 漏掉 crop 时第一条仍然是绿的——两条路径一起不裁，一致性照样成立 |
| 取帧映射 / EDL / 多轨 / 转场 / **定格** | **多片段一致性自检**（62 项）：五片段 + 空档 + 一个交叉溶解 + 一个 shader 擦除 + **一段定格**整条导出后比对 15 个取样帧。**定格那 8 条**（**D48**）：F[340,400) 整段定在源片第 150 帧，两个取样帧刻意隔 50 帧——既有的三条断言（导出取到第 150 帧 / 预览与导出一致 / 留边一致）对两帧各成立一次，外加一条"两帧是同一张画面"（实测 Δ **0°**）和一条**对照**（同两帧不定格时 Δ **48°**，证明这个量法看得见帧差）。定格帧刻意**不取源片第 0 帧**：0 会让 `frameToMicros(sourceIn, …)` 恒等于 0，"取帧位置来自 `sourceIn`"这件事就被乘以零、测不到。**两个探针必须隔远**是注入验出来的：删掉 `sampling.ts` 的恒定分支时，靠后那帧读到第 204 帧（红），而**靠前那帧（+5）没红**——5° 偏差被 20° 容差整个吸收；同一轮里「预览与导出一致」照旧全绿（两条路径共用同一个错的 `sampling.ts`）。转场那几条是**成对的**——「预览与导出一致」抓的是导出漏解第二层，「混合比例落在 (1-t)·出场 + t·入场 上」（参照值由两次纯层预览加权算出）抓的是进度或角色算错，而**只有前者时整个转场被丢掉仍然全绿**（两条路径共用同一份取样映射，一起丢掉当然还是一致）。**shader 转场**用 D6 的分区测量：擦除边界左块整块是入场层色相、右块是出场层的，**两条路径各断言一次**，外加一条"两侧确实不同"——把 shader 转场退化成溶解时右侧那条仍然是绿的（混合色恰好离出场层不远），只有这一条一次抓住 |
| 合成层 / Pixi 后端 / 调色 / LUT / shader 转场 / **源尺寸切换** / **裁剪** | **PixiJS 后端 spike**（34 项）：Worker 里起 WebGL，与 Canvas2D 跑同一份输入比对，外加三类只有 WebGL 才有的失效模式（跨 task 清空 / 上下文丢失**及其恢复** / **上下文预算**——断言的是复用常驻合成器这个生产架构，每轮新建作为对照印在同一行），**图层变换的摆位**（5 个用例 × 两个后端，比对手算位置），**一级调色**三条（GPU 像素 == `colorMatrixOf()` 的 CPU 参照实现 / 每个用例真的改变了像素 / 调色之后的恒等帧回到原色），**LUT** 四条（GPU 查表 == `sampleLutTexture()` / 恒等 LUT 不改画面 / 调色与 LUT 的顺序 / 跨帧不残留），以及 **shader 转场**四条（GPU 混出来的像素 == `mixTransition()` 的 CPU 参照，**15 个用例逐通道完全相同** / 羽化带上的像素确实是混出来的 / 擦除·圆形张开·推移在各自取样点上给出不同像素 / **故障：同一进度上不同的带给出不同像素**——钉的是整数哈希真的在分带），以及 **源尺寸在同一槽位上切换**两条（**D36**）：与 Canvas2D 逐条一致 / 换了尺寸之后留边跟着换。顺序是**大 → 小 → 大**——只测一个方向的话"从小到大"那一半是对的（第一版实测正是这样），会让人以为整件事没问题；以及**裁剪**三条（**D46**）：与 Canvas2D 逐步一致 / 留边按裁完的宽高比算 / **取的是那一块画面**（裁掉左半只剩右边那块）。第三条的期望值是探针图里那个 RGB **字面量**，不换算成色相——写死 240° 而真实 233.3° 的那 7° 会被容差吸收；而且量的是**画面正中一小块**（`measure()` 的平均色只避开上下黑边，裁剪把留边搬到左右之后读数等比暗 9%）。步骤是**不裁 → 裁 → 裁另一边 → 换方向裁 → 回到不裁**，每一步各自抓什么是**注入验出来的**，见 `compareCrop` 的文件头 |

**改自检源码之前先确认没有自检正在跑。** Vite 的 HMR 会**全量刷新所有开着的标签**，于是 `?autorun=...` 的页面全部重跑并 POST 一份新报告。踩过：Safari 正在跑长片轴时我改了源码，四份 Chrome 报告混进 `.reports/`，而我的等待脚本按文件名匹配、当场被骗，同时 Safari 那轮也被刷掉重来。等报告要**按 UA 过滤**，不能只看文件名。

**同一条的更坏形态：真机连着的时候不要分步编辑。** HMR 会把**两次编辑之间那个半成品**推到手机上。踩过：先改回调、再补变量声明，中间那一瞬手机正好跑了一轮，报告回来是 `ReferenceError: Can't find variable: lastStepAt`——看着像自检有 bug，其实那份代码从来没存在过完整版。手机上跑之前先把编辑做完、门禁跑过。

**长度轴要按"一档一页"跑，`&only=<秒>` 就是为这个加的。** 整条阶梯从头跑起时，跑到第 N 档已经背着前 N−1 档的资源账，于是**"这一档本身太长"和"跨档累积"永远分不开**。面板上也有对应的单档按钮。（曾拿"单独跑 10 分钟通过、阶梯里同一档 7.6 秒就 `Decoder failure`"当这条的证据，**那个对比不作数**——阶梯那次跑在被并行自检污染的窗口里，见下面"一台设备上同时只能跑一轮"。）

**一台设备上同时只能跑一轮自检，而这条纪律原来只靠自觉。** 同页并行跑两轮长片自检的后果是两份**都不抛错的假读数**：后开那轮把 `worker.onmessage` 顶掉，先开那轮再也收不到进度、90 秒后把一次**正在正常推进**的导出判成"死等，停在 decode:V1 第 600/900 帧"；紧接着它的看门狗发出 `cancel`，把**另一轮**正在跑的导出掐了，那一档报"导出被取消"而它自己谁也没取消。我据此把"iPhone 上一个页面导不了几次"当成了设备的墙，而 24 次连导全过直接否掉了它。现在 `client.ts` 在入口就拒掉同页第二次导出（报错而不是串线），协议里加了 `RunId` 兜跨时间的迟到 cancel。**注意 `?autorun=` 的页面跑完不会关掉，面板上的按钮照样能点**——那正是撞出这一条的姿势。

**这一条又咬过一次，而且形态完全不同：假红，且长得像刚改的那行代码。** 做自定义字体时我留着一个开着编辑器（因而一直握着一个 WebGL 上下文）的标签，又开了 M0 和预览两个 `?autorun=` 标签，于是预览一致性自检**红 4 条**，其中"带关键帧的摆位"报的是 `320/320/320/320`——四边留白全等于半个画布，也就是**预览那一帧什么都没画出来**。改动正好在文字栅格化上，第一反应就是"我把预览搞崩了"。关掉其余标签**单独重跑，19/19 全绿**。会这样是因为**上下文预算驱逐的是最老的那个**（见合成层约定），而最老的正是那个一直开着的编辑器标签。判据只有一条：**红了先单独重跑一遍**，别拿并行跑出来的读数去查代码。

**死等诊断必须带页面可见性。** 浏览器挂起不可见页面时，表现和真卡死**一模一样**：进度不动、心跳也不来。手机上自动锁屏就够触发。实测误报过一次——iPhone 10 分钟档报"死等 552 秒"，而诊断里 `此刻页面不可见、期间隐藏过 1 次` 直接点明是灭屏，紧接着重跑就 7.24× 过了。**产品侧的提示 gate 在可见性上**（不可见时停住是正常的），**自检侧不 gate 而是照报**——"它在页面被挂起时停住了"本身是读数，悄悄忽略会让阶梯一直卡在同一档却没有解释。

**而可见性这个判据本身不够——它靠的是收得到 `visibilitychange`，被挂起的那一侧恰恰可能收不到。** 实测报出过"此刻页面可见、期间隐藏过 0 次"的死等，而同一时刻另一轮自检正在跑导出，页面显然不可见：那条事件在 JS 被冻住时没送达，回到前台时也只补了个"变回可见"，而计数器只数"变成不可见"。**自证的是看门狗定时器的跳间隔**——JS 被冻住时它不跳，而这个事实不需要任何事件配合。所以一跳超过 3 秒就判"刚才被挂起"，把空闲计时从此刻重算，并把**被冻住几次、共多久**印进死等的报错里（`verify-device.ts` 的 `TICK_STARVED_MS`，`client.ts` 那条提示同理）。判据是"看门狗被冻住 0 次"——只有这样那 90 秒才算真的一直在看着。**同"先确认量法量的是被测对象，而不是量法自己的偏差"**：这一条正是靠它才认出来的，而在此之前每一条"死等"读数都不干净。

**自检里"跑完就删成品"的地方，长度轴上会反过来咬。** 被系统杀掉的那一轮不会删,于是 OPFS 里攒着几十 MB 的半成品,下一轮直接撞 `unknown transient reason (e.g. out of memory)`。真机上原样重演过一次(30 分钟成片 55.7MB + 一次被杀的尝试)。`?autorun=clear` 清。

**跑自检要起一个独立 profile 的浏览器实例，不要往正在用的那个里开标签。** `open -a "Google Chrome" <url>` 会复用用户手上那个实例，于是收尾时只剩"杀整个 Chrome"这一个钝工具——把别人正在用的窗口一起关掉。做法是自己起：

```bash
CHROME_DIR="$SCRATCH/chrome-kerf"
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --user-data-dir="$CHROME_DIR" --no-first-run --no-default-browser-check \
  "http://localhost:5173/?autorun=m0" &
pkill -f "user-data-dir=$CHROME_DIR"   # 只清自己这一份
```

独立 profile 顺带把 **OPFS / IndexedDB / localStorage 也隔离**了：导出残留、崩溃恢复快照、吞吐样本都不会漏到日常浏览器里。`--incognito` **不行**——它在同一个进程里开窗口，杀进程照样连坐。而"同时只能跑一轮"和"WebGL 上下文预算"两条要的都只是**没有别的 Kerf 页面开着**，从来不需要杀掉整个浏览器。

**跑自检不用手点，也不用截图。** URL 上加 `?autorun=m0`（或 `pixi` / `timeline` / `preview` / `device` / `length` / `clear`）会自动跑完并把**完整结果**POST 到 `.reports/`（`src/dev/autorun.ts` + `vite.config.ts` 里的 `reportSink`，都只在 dev 存在）。这条通道是查 Safari 那个 9.3ms 时做的，当场就回本了：它让"在 Safari 里跑一遍并拿到所有诊断字段"从"开浏览器 + 截图 + 人工转录"变成一条命令（`open -a Safari "http://localhost:5173/?autorun=m0"`），而 Safari 的自动化要先在设置里手动开开关。真机同理——手机上点一次，结果直接落到本地文件，**截图会截断的诊断字段不会丢**。

还有**第五和第六个，都不在这张表里**：`dev/verify-device.ts` 里的「真机自检」（分辨率轴）和「长片自检」（长度轴，`runLengthReport` / `?autorun=length`，可带 `&max=<秒>` 只跑前几档）。前四个问"代码对不对"，答案在所有机器上都该一样；它问**这台设备的上限在哪**，答案本来就因机而异，所以**不进回归门禁**，只在需要一台新设备的数字时手跑。要用 `pnpm dev:device` 起服务再从手机上开——**WebCodecs 和 OPFS 都要求安全上下文**，`localhost` 算、`http://192.168.x.x` 不算，直接开局域网地址会得到"没有 VideoEncoder"，看起来像"这台设备不支持"。

**两根轴不能合并成一条阶梯。** 分辨率顶的是画布、渲染目标、编码器缓冲（全在输出侧，一帧就到峰值）；长度顶的是随片长累积的东西。混着扫的话崩了都说不清是被哪一头顶掉的，而且会让"看一眼这台设备的分辨率上限"从十几秒变成十几分钟。长度轴的素材**接成很多 10 秒片段而不是一条长素材**：生成 30 分钟素材本身要跑很久，而"上百个片段"恰恰是长项目的真实形态。

**死等比崩溃更坏，而且不在同一套护栏里。** 分辨率轴假设"扛不住 = 标签页被杀"，于是靠 localStorage 前置写入留痕。长度轴上 Safari 实测到的却是**死等**：0% CPU、几百 MB 常驻、不抛错、不崩溃、进度条永远不动。localStorage 那条记录仍然管用（重开页面能读到死在哪一档），但要**跑一轮短到一定能跑完的**才取得出来——`?autorun=length&max=30` 就是为这个加的。`watchForStall` 则把死等本身变成读数：盯**进度停了多久**而不是给整次导出设总时限，后者会把"30 分钟的片子本来就要跑十分钟"误判成故障。

**跨浏览器量吞吐时，标签在不在前台是必须控制的变量。** Safari 那次"30 分钟死等 + 吞吐随片长衰减到 3.37×"**几乎全是后台节流**：放到前台重量是 9.71× / 10.33× / 10.57×（接近 Chrome），30 分钟也跑完了。三条互相独立的证据：死等那次的报告在切回前台的**那一秒**落地；空转看门狗阈值 90 秒却报了 **239 秒**（主线程定时器被节流）；放弃那次导出时 Worker **正常收到并处理了 cancel**。我据此判过"卡在 Worker 侧"，**是错的**——而同一次里"不是 `OfflineAudioContext` 段数"那条排除是对的。**两条"已排除"里有一条错，不影响另一条对**，所以别因为结论翻车就把整轮排查一起丢掉。

**手机跑久了会降频，而降频和"资源累积"长得一模一样——跨时段比数字之前先确认设备状态可比。** 实测差点栽进去：整条阶梯跑到第 4 档时标签页被系统杀掉，`diedAt` 明确指着 `长片 30 分钟`，而同一档单独跑两小时前刚以 7.36× 通过——"跨档累积"看起来铁证如山。**拦住这个结论的是顺手看了一眼那一轮 30 秒档的吞吐：4.33×，而同机同档稳态是 7.2–7.36×**，说明被测环境已经不是同一个了。凉下来重跑，整条阶梯四档全过。所以真机上每一轮都要留一个**同档基准读数**（最短那一档就行）当"设备状态"的刻度，只比被测那一档等于把环境当常量。同上一条"标签在不在前台是必须控制的变量"。

**失败的产物不能删。** 自检第一版无论成败都 `removeExportFile`，于是"解回来没有视频轨"一出现，唯一能查下去的东西当场就没了。现在只在**验过**才删（阶梯失败即停，最多留一个）。同一类错误我在同一天犯了两次：另一次是每轮 `rm -rf .reports` 图干净，把 Safari 首轮的真实结果删了——报告文件带时间戳、刻意不覆盖，就是为了留住前后对比。

**"最后进到哪一步"这种标记，必须由独立心跳发，不能搭在业务上报里。** 导出的逐帧进度固定发生在 `await pumpAudio()` **之后**，那一刻标记还没被下一帧覆盖，于是**每条正常进度都带着 `audio`**——看着像定位信息，实际是个常量。我拿"把阈值改成 1ms 逼它开火"验过一次并判它可用，而那次读到的正是这个常量：**量法和被测对象没分开**。改成 Worker 里每秒一条心跳（`ExportProgress.heartbeat`）之后才真的能点到步骤，验法也换成**注入一个已知位置的 hang**（在 `encode` 第 100 帧塞 `await new Promise(()=>{})`），要求诊断报出那个位置。相应地判"卡住了"**不能看"有没有收到消息"**——心跳每秒一条，那样永远不会超时，要看 `encodedFrames` / `marker` 有没有变。

它有三条值得单独记的东西：

- **崩溃是读数，不是失败。** 最后一步逐级升高输出分辨率往死里逼，而移动端"扛不住"的形态是**操作系统直接杀掉标签页**：没有异常、没有 `unload`、没有任何机会写下结论。所以每一档**动手之前**先把"正在试 X"写进 localStorage，成功后再写"通过"；重新打开页面读到"正在试 2160p"而没有"通过"，那条记录就是答案。跑完再返回结果的写法，在最关键的那次运行里恰好什么都拿不到。
- **那里报的常驻量是源片侧的，不随输出分辨率变化。** `ResidencyReport` 数的是我们记过账的东西（解码帧 / 解码器 / Input），全在源片侧；而阶梯扫的是输出分辨率，合成器画布、渲染目标、编码器缓冲一个字节都没记进去。桌面实测五档**全是 1MB**。第一版直接把这个数印在每一档后面，读起来就是"4K 只要 1MB"——现在标明了它是源片侧的。输出侧内存没有 API 可问（`performance.memory` 只报 JS 堆，`measureUserAgentSpecificMemory()` 要 COOP/COEP），**唯一的读数就是标签页活没活下来**。
- **源片固定 640×360、只放大输出**，所以扫的是输出侧上限；"能不能解 4K 素材"是另一根轴，没测。源片跟着一起放大的话，量到的上限说不清是被哪一头顶掉的。
- **判据是把成片解回来量宽高和帧数，不是看字节数。** 第一版拿成片字节数当"分辨率生效了"的旁证，桌面上它随分辨率涨（26→280KB）看着挺好；到了 iOS Safari 五档**全报 16MB**（正好是 mediabunny `StreamTarget` 的攒批阈值），代理彻底失效**而阶梯照样全绿**——"4K 导出成功"这个结论当时根本没有证据支撑。现在每档都解回来比对宽高，并同时印管道报的和重读的两个字节数。**后续**：那个 16MB 不是"代理不准"，是**文件真被撑到了 chunk 边界的 bug**，2026-07-28 由长片轴查清并修掉（`chunked: false`，见导出层约定）。当时换判据换对了，但**没有追问代理为什么坏**，于是这条 bug 又活了一整个里程碑。

**iOS 18.7 / Safari 26.4 实测（2026-07-27）**：`effects=true`，Pixi/WebGL2 起得来；**Pixi spike 28 项全绿**，其中那条对照证实了 D15/D16——复用组的预览活着，**每轮新建的对照组"被驱逐，且救不回来"**，所以复用常驻合成器在 iOS 上同样是唯一解。输出分辨率**五档全过没崩**，但 2160p **慢于实时**（0.66×），1080p 有 3× 余量，所以"移动端要限分辨率"的依据是**速度不是内存**。M0 一度红 2 项，查清是**自检读法踩了 Safari 的 seek 时间戳**、不是产品（成片偏移实测全是 0），已修，Safari 与 Chrome 现在都 28/28，详见 PLAN.md §8 风险 2。会炸的是长片的**混音峰值**（桌面 30 分钟 989MB，全在主线程），而这条阶梯只跑 60 帧、完全测不到——**下一步该测长度，不是更高分辨率。** 数字和那个 16MB 的 bug 都记在 PLAN.md §8 风险 4。

第四个原本不是回归自检，而是**换渲染后端之前必须成立的前提**；那些前提已经成立并完成迁移（D15 / D16），它现在是回归护栏。只有 GPU 才能验的东西继续加在这里——调色和 LUT 都是（算法有单测，但"shader 有没有按那个算法算"单测够不着）。

**"两条路径比对"覆盖不了渲染算法本身。** 把 shader 写错时第二个自检是**绿的**——预览和导出共用同一个（错的）shader，一致性当然成立。只有第四个里那条"GPU 出来的 == CPU 参照实现算出来的"会红。所以每加一种 GPU 效果，都要同时加一份能在 JS 里跑的参照实现，见 `compose/color.ts` 与 `compose/lut.ts` 的文件头。

多片段自检能断言"取到的是源片第 N 帧"，靠的是测试素材背景色相随帧号线性渐变（`hue = i/frames*300`）——色相编码了源片帧号。**改 `make-sample.ts` 的配色就要同步改 `measure.ts` 的 `sampleHueAt`**，否则自检开始误报。

音频同理：缺省的 `audioShape: "beeps"`（每秒一声 1kHz、其余静音）是 M0 第 17 项那条起始沿断言的依据，**不要动它**；量增益包络要用 `"tone"`（全程连续音）——稀疏提示音在半秒的淡化窗口里可能一声都没有，那时 RMS 全是 0，几条断言会**同时通过**，测的是运气。反过来，判**"取到了源片哪一刻"只能用 `beeps`**：`tone` 全程都在响，从哪儿取样听起来都一样，于是"裁过入点的片段取错了内容"在连续音上完全测不出来（D35 的配乐用 `makeSampleAudio({shape:"beeps"})` 正是为此）。**稀疏音上不能用 RMS 判"有没有声音"**——40ms 的窗口很可能整个落在两声之间，那时正确的成片也报 0；用峰值（`peakBetween`）。

**给音频断言做隔离，比给画面更重要。** 交叉淡化的成片是两层相加，而等功率的意义恰恰是"和是平的"——直接量**看不出任何一条曲线**。M0 那一段的做法是让每个窗口里只有一层有声音（三片段：有声 / 无音轨素材 / 有声），于是左交界量到的纯粹是淡出曲线、右交界是淡入曲线。用 `withAudio:false` 的素材而不是一段静音素材，是因为前者连排期都进不去（`planAudioJobs` 跳过没有音轨的源片），更干净。

**文字会污染这套断言**（`measure()` 取的是画面区平均色），所以 `measure()` 有可选矩形参数做**分区测量**（**D6** 已落地）：背景区继续断言色相，文字区单独看。自检素材里的文字用纯色实心块（`█`），不用真实文案；真实文案的排版质量靠断行单测 + 截图肉眼比对。

**给自检选取样区时要先问"这块里除了被测对象还有什么"。** 第一版把文字取样区放在画面区里，量到的非黑像素其实来自视频——把文字整层删掉两条断言照样绿。现在文字放在下方留边的黑区里，去掉文字层该区域 `maxChannel` 会归 0。

另外预览 seek 要落在**帧中点**（`sourceCenterMicrosAt`），落在帧起点会拿到前一帧。

## 性能对比要在贴近真实的参数下测

**规模不对的基准量到的是固定开销，不是被测对象。** Pixi spike 第一版在 320×320 上跑，结论是 Pixi 比 Canvas2D 慢 **2.11×**；换到 1280×720 不缩放之后是 **0.97×**。小画布上每帧的固定开销（画布 → `VideoFrame` 的捕获、命令提交）占了大头，编码几乎没参与——那个 2.11× 量的是开销比例。

这类错误的代价不是跑错一次测试，是**因为一个便宜但不真实的基准砍掉正确的技术方向**。所以：

- 分辨率、帧数、是否缩放都要贴着导出的常态取，不要为了自检跑得快而缩小。
- 输入要**逐帧不同**。重复同一帧会让 H.264 编出极小的 P 帧，把编码成本压没，从而放大其它环节的占比。
- 只测"提交耗时"没有意义。GPU 是异步的，`renderer.render()` 返回时活还没干完——要计时就得测到真正强制同步的那一步（这里是 `CanvasSource.add()` 里的 `new VideoFrame(canvas)`）。

## 波形约定（M2 末，D29 起）

- **波形解原片，不解代理**——代理转码把音轨整个丢掉了（`proxy.worker.ts` 的 `audio: { discard: true }`），所以这里没得选。好处是**不必等代理就绪**，导入之后就能开始；音频解码只解音轨的包，比抽缩略图便宜得多。
- **峰值按时间取桶，不按帧**。源片帧率和时间轴帧率可以不同，用帧号取桶会让 25fps 素材放在 30fps 时间轴上时波形整体拉伸 20%（同"不要用 `toSourceFrame()` 做取帧位置"）。所以 `drawWaveform` 收的是**秒**：源片入点秒数由 `sourceIn / 源片栅格` 算（栅格问 `sourceGridFps()`，纯音频素材没有自己的帧率、用项目帧率），片段长度秒数由 `帧数 / 时间轴fps` 算——**两个 fps 不是同一个**，混用不报错。
- **桶数不随缩放变化**（目标 200 桶/秒、上限 12 万）。按缩放缓存的话拖一下缩放滑块就要重解一遍整条音轨。缩小时一个像素跨多个桶取最大值（那正是峰值包络该有的样子），放大时一个桶铺多个像素、看起来是方块——那是常态不是 bug。
- **零宽区间至少取一个桶**（`peakBetween`）。放大到极限时相邻两个像素落进同一个桶，`ceil - 1 < floor`；返回 0 会让波形上出现随机空洞，看着像素材本身有断音。
- **缓存的值可以是 `null`，而"没试过"和"试过没有音轨"必须分开**（`waveformSettled()`）。合并成一个的话，一个没有音轨的素材会在每次重绘时都被重新解一遍。
- **一个音频包只 `toAudioBuffer()` 一次**。写在声道循环里就是每声道各建一份，立体声直接翻倍。
- **音量包络和波形画在同一张 canvas 上，纵轴是 0..2 的绝对刻度 + 一条 100% 参考线**。非线性刻度能把 100% 顶到上面去、更好看，但那样两个音量的高度差就不再对应它们的比值。**恒等音量不画线**——每个片段都横一条毫无信息的线，反而看不出哪个调过。包络按**像素**取样而不是只连关键帧：关键帧之间可以有缓动，只连点就画成了直线，而听到的是曲线。

## 首屏体积

**mediabunny（约 500KB）不能进主 chunk。** 它只能出现在 Worker 里，或被动态 `import()`。已经踩过两次：一次是 `probe`/`capability`/`thumbnails` 静态 import，一次是导出面板经 `client.ts` → `mixdown.ts` 把它拖回来。

**这条纪律现在由构建自己执行**（`vite.config.ts` 的 `bundleGuard`，D38）：被拖进入口 chunk 就直接构建失败，不再靠人记得去看一眼产物。**判据是模块图（`chunk.moduleIds`），不是在产物里 grep 包名**——那个代理两个方向都是坏的，实测：`grep -c mediabunny` 在**真的就是 mediabunny 的那个 chunk**（311KB 的 `id3-*.js`）上同样返回 **0**（压缩把包名整个丢掉了），所以它是一条**恒为真的假断言**，无论有没有被拖进来都报"没有"；反过来 `pixi` 在干净的主 chunk 里出现 5 次，全部来自我们自己的动态 import 路径和 `backend: "pixi"` 这个字符串。这个假判据在仓库里以"`grep -c mediabunny` → 0"的形式当过一次验收证据。**同那条"代理坏掉的时候要问代理为什么坏"**：坏的原因是压缩会丢包名，而模块图不会。

断言还**自证**：整个产物里一个受管库的模块都找不到时也要红（依赖换名、路径形状变了、被整个树摇掉），否则它会安静地退化成上面那个 grep 的下场。三条失败路径都反向验证过——注入静态 import 时入口从 361,867 B 涨到 748,428 B 并报出库名；受管列表里放一个不存在的库时报"判据坏了"；入口判据写错时报"找不到入口"（这条是自然触发的）。

**构建给 Pages 时要带 `--base=/kerf/`，而这个参数只在工作流里给**（D51）。它不写进 `vite.config.ts`，否则开发服务器一起搬到 `/kerf/` 下；而**给错时构建照样成功**，只有部署到子路径之后才表现成白屏（脚本 404）。所以 `pages.yml` 的组装步骤里有一句 `grep -q 'src="/kerf/assets/'` 当闸门。**别改成 `base: "./"`**：worker 和动态 import 的 URL 是另一套解析，显式给 base 之后产物里那三个 worker URL 是看得见的字面量（实测 `"/kerf/assets/proxy.worker-*.js"`），相对 base 下它们要靠运行时解析对——那是一条只能在部署之后才验得到的路。

拆分模式是**把"要 mediabunny 的那一半"单独成文件**：`capability.ts` / `capability-probe.ts`、`thumbnails.ts` / `thumbnail-extract.ts`。同步渲染路径上的函数（如 `drawStrip`）不能 await 动态 import，所以必须靠文件边界隔离，不能靠调用点。

**PixiJS 同理（412KB raw / gzip 119KB）。** 但它用的是另一个模式：`pixi-compositor.ts` 对 pixi.js 只有 `import type`，动态 `import()` 藏在 `createPixiCompositor()` 这个**异步工厂**里，返回的 `composeFrame()` 是同步的。边界划在"创建时"而不是"调用时"，因为调用点在 rAF 回调和导出逐帧循环里，每帧 await 不可接受。接 Pixi 那一轮验的是主 chunk **一个字节都不变**（252,997 B / gzip 80,027，见 PLAN.md §7 M1.6）。之后 M2/M3/M4 的功能陆续加进来，**当前是 361,867 B（gzip 119,072）**——这个数只是读数，判据是上面那条模块图断言：绝对值会随功能正常长大，"被拖进来了"是结构性错误，两件事不能用同一个阈值判。

## 技术栈（已定，不要另选）

| 用途 | 用什么 |
|---|---|
| 编解码 / 封装 / 转码 | mediabunny |
| 画面合成 / 滤镜 / 转场 | PixiJS v8（锁 WebGL2，不用 WebGPU；M2 后半段接入） |
| 音频离线混流 | OfflineAudioContext |
| 状态 / 撤销栈 | Zustand + Immer |
| 大文件存储 | OPFS + File System Access API |
| 预览代理转码 | mediabunny `Conversion`（Worker 内，串行） |
| 时间轴、检查器、波形、导出面板 | 自研 |

ffmpeg.wasm 不进主路径（软编慢 10–50×，多线程版的 COOP/COEP 会打断页面内第三方脚本）。要补 AAC 只考虑 libav.js，且属于待定项。

## 硬规则

这几条错了要重写大片代码，动到相关地方先对照一遍：

1. **时间计算一律用有理数帧号（`num/den`）+ 微秒时间戳。禁止用浮点秒做帧运算。** 浮点秒只允许出现在调用 mediabunny（以秒为单位）的那一行。另外：拿"算出来的秒"和"解码器给的 `sample.timestamp`"比较时，必须带上 `FRAME_ALIGN_EPSILON_SECONDS` 容差，否则 trim 末帧会静默少一帧。
2. **预览和导出必须共用同一个 `compose(edl, frame)`。** 不允许出现两套渲染路径——这是"预览和导出画面不一致"的唯一根因。
3. **导出取帧必须用 `VideoDecoder` 顺序解码。禁止用 `video.currentTime` seek。** 它不帧精确，且锁在 1x 实时速度。预览走 seek + 代理文件没问题。
4. **每个 `VideoFrame` 都必须 `.close()`。** 它是 GPU 资源，漏一个几秒内 OOM。走池化的"借出—归还"，不要靠 GC。
5. **必须 `await source.add(...)`。** mediabunny 的 add() 在编码器就绪时才 resolve，await 它就是背压；不 await 会无限堆积帧直到 OOM。不需要自己轮询 `encodeQueueSize`。
6. **解码 / 合成 / 编码全部在 Web Worker。** 主线程只跑 UI，否则导出期间界面完全卡死。注意 **`OfflineAudioContext` 在 Worker 里不可用**：M2 做多轨混音时 PCM 要在主线程算好再 transfer 进去。
7. **裁剪要处理 GOP 边界**：clip 起点通常不在关键帧上，必须回退到前一个 keyframe 解码再丢弃多余帧，否则 trim 花屏。用 `VideoSampleSink.samples(start, end)` 顺序解码，mediabunny 内部已做这件事；不要逐帧 `getSample()`，那会反复 seek，慢一个量级。
8. **时间轴时长按视频轨算**，不能用 `input.computeDuration()`——AAC 的 priming/padding 会让音轨更长，300 帧素材会被算成 303 帧。
9. **导出结果流式写盘，不要攒成 Blob。** 已落地：`StreamTarget` + `FileSystemWritableFileStream`，picker 拿到的句柄可直接 postMessage 进 Worker；没有 picker 的浏览器回退成"流式写 OPFS 再触发下载"。不要为了方便退回 `BufferTarget`。
10. **不静默降级导出格式。** 能力不足时明确告知用户后果并给出路——用户点了 MP4 却拿到 WebM 是投诉源。相关设计见 PLAN.md 的 D3。

## 改动设计时

界面设计决策记在 PLAN.md §6，四段格式（决定 / 理由 / 放弃了什么 / 什么情况下重新评估）。**改设计前先读那一节**——尤其"放弃了什么"，避免重新提一个已经被否掉的方案。新决策追加进去，别另起文档。
